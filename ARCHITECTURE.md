# حدود كَحيل مواعيد المستقلة

- الفرع شجرة تطبيق مستقلة ولا يُدمج مباشرة مع `main`.
- وضع العرض يعمل محليًا في المتصفح ويحفظ حالته في `localStorage`.
- مخطط Supabase المرفق مستقل ويستخدم البادئة `appt_` فقط.
- لا توجد قراءة أو كتابة أو مفاتيح أجنبية إلى جداول سوق كَحيل.
- بيانات موقع العميل مؤقتة داخل جلسة البحث ولا تُحفظ.
- موقع مقدم الخدمة يحفظ في قاعدة المواعيد المستقلة فقط بعد موافقته.
- الربط المستقبلي مع السوق محصور في رابط/معرف خارجي، ثم يمكن استبداله بـSSO أو API موقّع.
- عند إنشاء مستودع Supabase مستقل، تطبق migration وseed المرفقتان هناك، وليس على مشروع السوق.
