# تدقيق المسارات والوصول والعزل — 02/08/2026

الهدف: خريطة مسارات واحدة، منع التكرار، وفرض الصلاحيات والعزل عند فتح أي رابط مباشر.
لم يُحذف أي مسار، ولم تُعدّل قاعدة البيانات ولا سياسات RLS، ولم يُنشر التطبيق.

## 1. المصدر الوحيد للحقيقة

`src/lib/routes-map.ts` يسجّل كل مسار مع: النوع، اشتراط الدخول، اشتراط حساب نشط،
أنواع الهوية المسموحة، الصلاحية المطلوبة، القالب، والمسار المعتمد للبديل القديم.
الملف لا يمنح أي وصول؛ الصلاحية النهائية تبقى في قاعدة البيانات
(RLS + `mkt_my_accounts` + `mkt_account_context` + `has_perm`).

## 2. تصنيف المسارات

| المسار | النوع | دخول | حساب نشط | الهوية | الصلاحية |
| --- | --- | --- | --- | --- | --- |
| `/`, `/search`, `/categories/$slug`, `/ads/$slug`, `/businesses/$slug`, `/u/$username` | عام | لا | لا | الزوار وغيرهم | — |
| `/auth`, `/register`, `/invite/$token`, `/verify-invoice` | عام | لا | لا | الزوار وغيرهم | — |
| `/more` | عام (روابطه تُرشَّح) | لا | لا | الجميع | حسب الرابط |
| `/choose-account`, `/market-setup` | يتطلب دخولًا | نعم | لا | فردي/منشأة | — |
| `/dashboard/*` (السوق) | حساب نشط | نعم | نعم | فردي/منشأة | حسب الصفحة |
| `/dashboard/business` | حساب نشط | نعم | نعم | منشأة فقط | — |
| `/select-account`, `/me`, `/settings`, `/onboarding`, `/notifications` | يتطلب دخولًا | نعم | لا | فردي/منشأة | — |
| `/projects*`, `/requests*`, `/suppliers`, `/products`, `/invoices*`, `/custody`, `/my-custody`, `/supervisors*`, `/team`, `/users`, `/invitations`, `/reports`, `/audit`, `/trash`, `/portal`, `/dashboard` (التشغيلي) | تشغيلي داخل الكيان | نعم | نعم | فردي/منشأة | `projects.view`، `requests.view`، `invoices.view`، `custody.view`، `users.manage`… |
| `/admin`, `/admin/*` | إدارة المنصة | نعم | لا | مسؤول منصة | فحص خادمي في `AdminShell` |
| `/marketplace` | قديم/مكرر | — | — | — | يوجّه إلى `/` |
| `/login` | قديم/مكرر | — | — | — | يوجّه إلى `/auth` |

## 3. التكرار ومعالجته

- `/marketplace` كان يعرض نفس صفحة `/` حرفيًا → أصبح توجيهًا دائمًا إلى `/`
  مع الحفاظ على مَعلمات البحث، وتسجيل محلي لعدد مرات الاستخدام
  (`logLegacyRoute`) حتى يمكن سحبه لاحقًا بأمان. كل الروابط الداخلية تشير الآن إلى `/`.
- `/login` يبقى توجيهًا إلى `/auth` مع الحفاظ على `next`.
- `/select-account` (تشغيلي، اختيار الكيان) و `/choose-account` (السوق، اختيار الحساب)
  مسارَان لغرضين مختلفين؛ أُبقيا كما هما ووُثِّق الفرق ولم يُدمجا في هذه الدفعة.
- `/portal` (بوابة المشرف) و `/dashboard` (لوحة الكيان) مسارَان لدورين مختلفين؛ أُبقيا.

## 4. فرض الوصول عند الرابط المباشر

- `/_authenticated/*`: بوابة موجودة تتحقق من الجلسة قبل تحميل الصفحة، والقائمة
  الجانبية تُرشَّح بالصلاحيات (`can`).
- `/admin/*`: أُضيفت بوابة موحدة `src/routes/admin/route.tsx` تشترط جلسة قبل تحميل
  أي صفحة إدارة، مع `next` للعودة؛ فحص مسؤول المنصة يبقى داخل `AdminShell`.
- `/dashboard/*` (السوق): `DashboardShell` يقرأ الآن قاعدة المسار من الخريطة ويتحقق
  من نوع الهوية والصلاحية المطلوبة إضافةً إلى وجود حساب نشط.
- `/more`: روابطه تُرشَّح عبر `canSeeLink` فلا يُعرض رابط لا يمكن فتحه.

## 5. رسائل المنع الموحدة

`src/components/AccessDenied.tsx` شاشة واحدة لكل الحالات: انتهاء الجلسة، غياب
الحساب النشط، عنصر يتبع حسابًا آخر، ميزة غير متاحة، أو منع صلاحية. لا تذكر اسم
الكيان المالك ولا الجدول ولا السياسة — فقط الخطوة التالية (تسجيل الدخول، تغيير
الحساب، الرئيسية).

## 6. ما لم يُنفَّذ في هذه الدفعة

- لم يُحذف أي مسار (بما فيها `/marketplace` و `/login`؛ صارا توجيهًا فقط).
- لم تُعدَّل سياسات RLS ولا الدوال الخادمية ولا الروابط في قاعدة البيانات.
- لم يُدمج `/select-account` مع `/choose-account`.
- لم يُنشر التطبيق.

---

# إغلاق التدقيق — 02/08/2026 (متابعة)

## 7. خطأ `I18nProvider`

| البند | النتيجة |
| --- | --- |
| السبب الجذري | `notFoundComponent` و `errorComponent` في **جذر** المسارات يُعرضان **بدلًا من** `RootComponent`، لذا كانا خارج شجرة `I18nProvider` تمامًا؛ أي استدعاء `useI18n` داخلهما (أو داخل مكوّن مشترك يُعرض منهما) يقع بلا سياق. |
| الملف المتأثر | `src/routes/__root.tsx` (والهوك في `src/i18n/index.tsx`). |
| الإصلاح | كل من صفحتي 404 والخطأ الجذريتين تُغلَّف الآن بـ `I18nProvider` خاص بها، ومحتواها انتقل إلى `NotFoundView` / `ErrorView` مترجمين بمفاتيح `routeError.*` الجديدة في `ar.json` و `en.json`، مع `dir` من السياق. الهوك يحتفظ بقاموس احتياطي مطبوع النوع (`I18nContextValue`) لكنه يطبع تحذيرًا في DEV حتى لا يُخفي خللًا في النطاق. |
| بلا `any` / بلا تعطيل فحص الأنواع | نعم؛ `tsgo --noEmit` نظيف. |
| العربية/الإنجليزية | تعمل في: المسارات العامة (`/`, `/verify-invoice`)، `DashboardShell`، `AdminShell`، شاشة 403 (`AccessDenied`)، وصفحة 404 — تم التأكد أن EN تُعرض `dir=ltr` وAR تُعرض `dir=rtl`. |

## 8. مشكلتا Security

### 1) `Public Can Execute SECURITY DEFINER Function` (anon)
- **المصدر:** دوال `SECURITY DEFINER` في مخطط `public` كانت قابلة للنداء بلا تسجيل دخول.
- **التقييم:** مشكلة حقيقية جزئيًا (دوال Triggers وحُرّاس داخليون لا يجب أن يُنادوا مباشرة) + تحذير مقبول للأربع الدوال العامة المطلوبة.
- **الأثر المحتمل:** نداء مباشر لدوال حراسة/تطبيع داخلية من زائر.
- **الإجراء:** سُحبت `EXECUTE` من `PUBLIC, anon, authenticated` عن **كل** دوال Triggers، وسُحبت من `anon` عن كل دالة غير عامة. المتبقي عامًا بالتصميم فقط: `invitation_preview` (محدودة المعدل، لا PII)، `mkt_listing_is_public`، `mkt_increment_views`، `mkt_public_phone`.

### 2) `Signed-In Users Can Execute SECURITY DEFINER Function`
- **المصدر:** واجهة RPC للتطبيق (`mkt_my_accounts`, `mkt_account_context`, `create_workspace`, `change_request_*`, `can_*`, `has_role`, `has_perm`).
- **التقييم:** تحذير مقبول.
- **الأثر المحتمل:** لا شيء؛ كل دالة تستنبط المتصل من `auth.uid()` وتتحقق من العضوية والصلاحية داخليًا، و`SECURITY DEFINER` لازم لعدم كشف جداول البحث.
- **الإجراء:** سحب `EXECUTE` من `authenticated` عن دوال Triggers، وتوثيق القبول في security memory. **لم تُضعَّف أي سياسة RLS ولم تُمنح أي صلاحية عامة.**

## 9. اختبارات المسارات المباشرة

| الحالة | النتيجة |
| --- | --- |
| زائر → `/dashboard` | تحويل إلى `/auth` ✅ |
| زائر → `/admin` | تحويل إلى `/auth?next=%2Fadmin` ✅ |
| مستخدم بلا حساب نشط | بوابة `useRequireAccount` → `/choose-account` (والزائر → `/auth`) ✅ |
| حساب شخصي يفتح مسار منشأة | `DashboardShell` يقرأ `identity` من `routes-map.ts` → شاشة منع `unavailable` ✅ |
| عضو منشأة بالصلاحية | وصول طبيعي ✅ |
| عضو منشأة دون الصلاحية | شاشة منع `forbidden` (وروابط `/more` مُرشَّحة) ✅ |
| عنصر تابع لكيان آخر | صفحة العنصر تعرض «يتبع حساب آخر» + عرض التبديل إن كان له صلاحية، وإلا `notYours` ✅ |
| مسار غير موجود | 404 مترجمة، بلا انهيار ✅ |
| مسار ممنوع | 403 محايدة بلا كشف اسم الكيان ✅ |
| `/marketplace?q=abc` | → `/?q=abc` (Query Params محفوظة) ✅ |
| معرفات العناصر عند التوجيه | `/login?next=…` و `/admin` → `?next=%2Fadmin` محفوظة ✅ |

## 10. العزل الفعلي (ليس واجهة فقط)

اختبار مباشر على REST بمفتاح الزائر مقابل العدد الحقيقي في قاعدة البيانات:

| الجدول | صفوف فعلية | يراها الزائر |
| --- | --- | --- |
| `profiles` | 89 | 0 |
| `supervisors` | 31 | 0 |
| `projects` | 24 | 0 |
| `mkt_listings` | 3 (0 منشور) | 0 |
| `custody_transactions` | — | 401 (بلا صلاحية) |
| `mkt_my_accounts()` للزائر | — | 401 permission denied |

- **خلل مثبت وأُصلح:** `mkt_listings` كانت تسمح بتعديل `owner_user_id` و `tenant_id`
  بعد الإنشاء (سياسة التحديث تكتفي بعضوية الكيان)، أي إمكانية نقل إعلان بين
  الحسابات. أُضيف Trigger `aa_mkt_listings_freeze_owner` يرفض أي تغيير لهما.
- فقدان العضوية يمنع الوصول فورًا: كل السياسات تُقيّم `is_tenant_member(tenant_id)`
  لحظة الاستعلام، فلا كاش خادمي للعضوية.
- الحساب الشخصي لا يرى بيانات النظام التشغيلي: الاستعلامات مقيّدة بـ `tenant_id`
  والدوال `can_*` / `has_perm`، والواجهة تحجب المسار قبل ذلك.

## 11. البناء وفحص الأنواع والتجاوب

| الفحص | النتيجة |
| --- | --- |
| `tsgo --noEmit` | نظيف ✅ |
| `vite build` | نجح ✅ |
| العربية RTL | `dir=rtl` على كل المسارات المختبرة ✅ |
| الإنجليزية LTR | `dir=ltr` مع نصوص إنجليزية في 404 ✅ |
| 320px / 390px / 1366px | `scrollWidth - innerWidth = 0` على كل المسارات ✅ |
| أخطاء Console حاجبة | لا شيء؛ فقط 404 لمورد ثابت غير حاجب ✅ |

## 12. تحفظات متبقية

- `/select-account` (النظام التشغيلي) و `/choose-account` (السوق) ما زالا شاشتين
  منفصلتين — مقصود في هذه المرحلة لاختلاف نموذج الكيان، ويُدمجان لاحقًا.
- تحذيرا Security أعلاه مقبولان بالتوثيق، وليسا مغلقين تقنيًا: أي دالة عامة جديدة
  يجب أن تُراجع قبل منح `anon`.
- اختبارات الأدوار نُفّذت عبر منطق البوابات وسياسات RLS المُثبتة، لا عبر جلسات
  حقيقية لكل دور (لا توجد حسابات اختبار لكل صلاحية).

**النتيجة:** المسارات والعزل مغلقة. لم يُحذف أي مسار، ولم تُضعَّف أي سياسة، ولم يُنشر التطبيق.
