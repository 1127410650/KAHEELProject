# تدقيق: إعادة تصميم الصفحة الرئيسية العامة للسوق «كحلي»

## تصحيح 2026-08-04 (ب): بانر الإعلانات المميزة بدل بانر «اعرض إعلانك بسهولة»

### التغييرات
- حُذف نهائيًا `MarketHomeBanner.tsx` وصورته `src/assets/market/banner-post-listing.jpg`، وأُزيلت مفاتيح `market.home.banner.*` من `ar.json`/`en.json`. عبارة «اعرض إعلانك بسهولة» لم تعد موجودة في المشروع.
- أُضيف `src/components/marketplace/home/MarketFeaturedBanner.tsx` مباشرة بعد شريط التصنيفات: صورة غلاف حقيقية، شارة فضية «مميز»، العنوان، السعر والعملة، المدينة، زر «عرض الإعلان»، زر المفضلة، ورابط كامل إلى `/listing/$slug`.
- Overlay كحلي خفيف من الأسفل + نص أبيض، والفضي للعناصر المميزة فقط (لا ذهبي). ارتفاع الشريحة 200px على الجوال و320–360px على الكمبيوتر، سحب أفقي مع snap على الجوال، وأزرار تنقل جانبية على الكمبيوتر، ومؤشرات شرائح تظهر فقط عند وجود أكثر من إعلان مميز.
- حُذف قسم «إعلانات مميزة» المكرر أسفل «أحدث الإعلانات»؛ العنوان يظهر مرة واحدة فقط.
- الترتيب النهائي: الهيدر ← البحث ← شريط التصنيفات ← بانر المميز ← تصفح حسب الفئة ← أحدث الإعلانات ← الفوتر ← الشريط السفلي (جوال).
- مصدر التمييز هو حالة قاعدة البيانات فقط: `status='published'` و`deleted_at is null` و`is_featured` و`featured_until > now()` (نفس فلتر `featuredOnly` القائم). لم يُنشأ أي تمويل ولم تُخصم نقاط ولم تُعدّل بيانات QA — يوجد فعليًا 6 إعلانات مميزة سارية.
- عند غياب أي إعلان مميز فعلي يُخفى البانر بالكامل بلا بديل وهمي.
- لم تُلمس صفحة `/more` ولا الفوتر ولا نموذج إضافة الإعلان في هذه الجولة.

### الاختبارات
- `tsgo --noEmit`: نجح دون أخطاء. `vite build`: نجح.
- الرئيسية على 320×900، 390×852، 768×1024، 1024×768، 1366×768، 1440×900: لا Overflow أفقي (scrollWidth = عرض الشاشة في كل المقاسات)، لا صور مكسورة، لا Skeleton عالق، لا أخطاء Console، عنوان «إعلانات مميزة» مرة واحدة، وعبارة البانر القديم غير موجودة.
- حالة عدم وجود مميز (استجابة مميزة فارغة): البانر مخفي بالكامل بلا أخطاء (390 و1366).
- الفوتر يلي المحتوى طبيعيًا والشريط السفلي لا يغطي المحتوى (لقطة نهاية الصفحة 390).

### اللقطات
`/tmp/browser/home/screenshots/`: `home-320.png`، `home-390.png`، `home-768.png`، `home-1024.png`، `home-1366.png`، `home-1440.png`، `home-390-footer.png`، `home-nofeatured-390.png`، `home-nofeatured-1366.png`.

### عزل نسخة مدير النظام — غير مُثبَت في هذه الجولة
جلسة بيئة الاختبار مسجَّلة خروج (`signed_out`)، ولا تتوفر وسيلة لتسجيل الدخول بحساب مدير النظام من داخل بيئة التنفيذ، لذلك تعذّر التقاط `/admin` وصفحة إدارية فرعية على 1366px. لم تُعدّل أي ملفات إدارية في هذه الجولة (التعديلات محصورة في `MarketHome.tsx`، مكوّن البانر الجديد، تصدير `FavoriteButton`، وملفي الترجمة)، ولا توجد أنماط عامة جديدة. بمجرد تسجيل الدخول بحساب مدير النظام في المعاينة سأنفّذ اللقطتين والتحقق من الهيدر والقائمة الجانبية والألوان وغياب عناصر السوق.

## تصحيح 2026-08-04 (ج): الألوان، حاوية الكمبيوتر، الدخول المباشر، وتبديل الحساب من الهيدر

### 1. بقايا اللون البترولي
- فحص شامل لمصادر السوق: لا يوجد أي `teal` / `emerald` / `cyan` ولا أي كود ألوان مكتوب مباشرة (`#006B6D` أو غيره) داخل `src/components/marketplace/**` أو صفحات السوق. الهوية كلها عبر Tokens.
- فحص وقت التشغيل (computed styles) على الرئيسية في 320/390/430/768/1024/1366/1440 وعلى `/auth` وفي الوضع الإنجليزي LTR: **صفر** عنصر بلون بترولي/أخضر مزرق في `color` أو `backgroundColor` أو `border` أو `outline`.
- زر البحث، السعر، «عرض الكل»، الشارات، زر الإجراء الرئيسي وFocus Ring كلها كحلي/فضي (`--primary` = #0B1739، `--ring` = #182A55، الشارات على `--secondary` = #EEF2F5) عبر نطاق `.market-surface`.
- لم تُلمس ألوان الإدارة ولا النظام الداخلي: لا تعديل على أي Token في `:root`، والنطاق الكحلي يُطبَّق فقط على شجرة السوق.

### 2–5. البانر والترتيب
- البانر القديم وصورته ومفاتيحه محذوفة سابقًا؛ تحقق نصي في كل المقاسات: عبارة «اعرض إعلانك بسهولة» غير موجودة، وعنوان «إعلانات مميزة» يظهر **مرة واحدة** فقط.
- الترتيب المعتمد كما هو: هيدر ← بحث ← شريط تصنيفات ← بانر مميز ← تصفح حسب الفئة ← أحدث الإعلانات ← فوتر ← شريط سفلي (جوال). لم يُضف أي قسم آخر.

### 6. عرض الكمبيوتر
- حاوية مركزية موحدة `max-width: 1240px` مع `mx-auto` وPadding متوازن (`px-4 lg:px-6`) على الهيدر وشريط التصنيفات والبانر والفئات والبطاقات والفوتر — كلها داخل الحاوية نفسها.
- شبكة البطاقات: 1 على الجوال، 2 من `sm`, **3 من `md`**, 4 من `lg`، بفواصل أوسع على الكمبيوتر وعناوين أكبر (`lg:text-xl`). لا JavaScript لتحديد التصميم — Media Queries فقط.
- لا Overflow أفقي في أي مقاس (`scrollWidth <= innerWidth`).

### 7. الدخول المباشر دون شاشة اختيار حساب
- `landingPathForSession()` تعيد الآن `/` للمستخدم العادي (و`/admin` لمدير النظام)، بدل `/select-account`؛ ومسار `next` الآمن له الأولوية.
- `register.tsx` و`auth.tsx` لم يعد لهما أي توجيه إلى شاشة اختيار حساب.
- `useActiveAccount` تحلّ السياق تلقائيًا: تستعيد الحساب النشط السابق إذا ما زال صالحًا خادميًا، وإلا ترجع إلى **الحساب الشخصي** افتراضيًا (سابقًا كان يُختار تلقائيًا فقط عند وجود حساب واحد).
- `useRequireAccount` لم تعد تفرض شاشة اختيار: تبقى `/choose-account` كشبكة أمان فقط عند عدم وجود أي حساب متاح للمستخدم إطلاقًا، والمسار القديم `/select-account` ما زال توجيهًا آمنًا في `routes-map`.

### 8. تبديل الحساب من الهيدر فقط
- `AccountMenu` أصبح المحدد الوحيد: يعرض اسم الحساب النشط + أيقونة شخص/منشأة + سهم، ويفتح Bottom Sheet على الجوال وPopover على الكمبيوتر.
- داخله: الحساب الشخصي أولًا ثم المنشآت المصرح بها فقط (من `mkt_my_accounts` الخادمية)، علامة ✓ على الحساب النشط، نوع الحساب، «إنشاء منشأة جديدة» → `/business/new`، وروابط إدارة الحساب/المنشأة.
- التبديل في المكان: تحقق خادمي عبر `mkt_account_context` + `set_active_tenant`، منع الضغط المكرر بحالة `switching`, إزالة كل كاش الحساب السابق ثم `invalidateQueries()` (الإعلانات/المحادثات/التنبيهات/الصلاحيات)، والبقاء في الصفحة الحالية. عند الفشل: رسالة «تعذر تبديل الحساب» بدون تغيير السياق.
- لم تبق أي واجهة ثانية لاختيار الحساب بعد الدخول (لا رابط «تغيير الحساب» في القائمة).

### 9. صفحة الدخول
- كحلي/أبيض بالكامل: زر الدخول ورابط إنشاء الحساب وFocus الحقول كحلي (تحقق computed).
- لا شريط سفلي ولا فوتر سوق ولا محدد حساب على `/auth` (تحقق DOM).
- الكمبيوتر: الجزء الكحلي أصبح متوازنًا (شعار + عنوان + وصف في كتلة `max-w-md` بمنتصف العمود)، والنموذج `max-w-md` بدل `max-w-sm`، بتوزيع أعمدة 1.1fr/1fr.
- آلية الهوية/البريد/الجوال وكلمة المرور لم تتغير، ولا تُعرض أي أدوار داخلية.

### 11. نتائج التنفيذ
- `tsgo --noEmit`: نجح. `vite build`: نجح.
- Console: صفر أخطاء في كل المقاسات. لا صور مكسورة، لا Skeleton عالق، لا Redirect Loop.
- عميل Supabase واحد فقط (`@/integrations/supabase/client`) ومستمع جلسة واحد في الجذر — لم يُضف أي مستمع أو عميل جديد.
- لا تسرّب CSS إلى الإدارة: لم تُعدّل `styles.css` في هذه الجولة، والنطاق الكحلي مرتبط بـ `.market-surface` فقط.

### اللقطات المرفقة
`.lovable/audit/2026-08-04-kahli-home/`: `home_390x852.png`، `home_1366x768.png`، `home_en_1366.png`، `auth_390.png`، `auth_1366.png` (إضافة إلى لقطات المقاسات الأخرى وحالة غياب المميز في `/tmp/browser/kahli/shots/`).

### ما لم يُثبَت بعد (يحتاج جلسة)
جلسة بيئة التنفيذ ما زالت `signed_out`، لذلك تعذّر:
- لقطة الهيدر بحساب شخصي، ومحدد الحساب مفتوحًا، والهيدر بعد التبديل إلى منشأة.
- لقطة `/admin` الفعلية وصفحة إدارية فرعية على 1366px لإثبات عزل نسخة مدير النظام.
لم تُعدّل أي ملفات إدارية في هذه الجولة (التعديلات: `MarketHome.tsx`، `MarketShell.tsx`، `home/*`، `AccountMenu.tsx`، `mkt-account.tsx`، `mkt-platform.ts`، `auth.tsx`، `register.tsx`). بمجرد تسجيل الدخول بحساب مدير النظام في المعاينة سألتقط الأدلة الأربعة المتبقية.

---

## تصحيح الإغلاق الأخير (04/08/2026 — 22:3x)

### 12. تنسيق الأسعار حسب اللغة (منسق مركزي واحد)
- المصدر الوحيد: `priceLabel` في `src/lib/mkt.ts`. حُذفت `priceUnitPhrase` (كانت تولّد `per month` / `لكل شهر`).
- الصيغة المعتمدة:
  - العربية: `2,100,000 ر.س` — `45,000 ر.س / شهر` — `1,200 ر.س / يوم` — `28,000 ر.س / سنة` — `250 ر.س / ساعة`.
  - الإنجليزية: `SAR 45,000 / month` — `SAR 1,200 / day` — `SAR 28,000 / year` — `SAR 250 / hour`.
- الأرقام غربية 0–9 في اللغتين (`Intl.NumberFormat("en-US")`) دون تغيير.
- أُصلح موضعان كانا يتجاوزان المنسق أو يفقدان اللغة:
  - `ListingCard.tsx`: كان يستدعي `priceLabel` بدون `locale` فيطبع `45,000 SAR per month` داخل الواجهة العربية — هذا هو مصدر الخلط المُبلَّغ عنه. صار يمرّر لغة الواجهة.
  - `ListingForm.tsx` (شاشة المراجعة قبل النشر): كان يركّب `${price} ${currencyCode}` يدويًا؛ صار يستدعي `priceLabel`.
- بقية الأسطح تستهلك المنسق نفسه عبر `ListingCard` أو `priceLabel` مباشرة: بانر المميز (`MarketFeaturedBanner`)، أحدث الإعلانات، البحث `/search`، التصنيفات `/categories/$slug`، المفضلة، `/dashboard/my-ads`، تفاصيل الإعلان `/ads/$slug` والإعلانات المشابهة، الملف العام للفرد `/u/$username` والمنشأة `/businesses/$slug`.
- تحقق آلي على 390×852 و768×1024 و1366×768 في اللغتين: صفر ظهور لـ `SAR per` / `per month` / `per day` / `per year` / `per hour` في الواجهة العربية.
- عينات مقروءة من DOM بعد التصحيح:
  - عربي: `2,100,000 ر.س` ، `45,000 ر.س / شهر` ، `1,800 ر.س / يوم`.
  - إنجليزي: `SAR 2,100,000` ، `SAR 45,000 / month` ، `SAR 1,800 / day`.

### 13. حالة غياب الإعلانات المميزة
- طريقة الاختبار: تعطيل `is_featured` مؤقتًا لإعلانات QA فقط (`qa_batch_id is not null`) ثم إعادتها كما كانت. لم تُمسّ أي بيانات حقيقية، والتحقق بعد الاستعادة: 6 مميزة / 34 منشورة كما قبل الاختبار.
- النتيجة (390 و1366): بانر «إعلانات مميزة» غائب كليًا بلا فراغ مكانه، «تصفح حسب الفئة» باقٍ، «أحدث الإعلانات» يعرض الإعلانات العادية (6 بطاقات)، ولا تظهر رسالة «لا توجد إعلانات منشورة حاليًا».
- لقطات: `nofeatured_1366.png` ، `nofeatured_390.png`.

### 14. الفحوصات
- `tsgo --noEmit`: نجح. `vite build`: نجح.
- 390×852 / 768×1024 / 1366×768 × (عربي، إنجليزي): Overflow أفقي = 0، صور مكسورة = 0، Skeleton عالق = 0، أخطاء Console = 0.
- لم يُغيَّر أي لون أو ترتيب أو مقاس: التعديلات محصورة في `src/lib/mkt.ts` (منطق نص السعر)، وسطر واحد في `ListingCard.tsx`، وسطرين في `ListingForm.tsx` (استيراد + شاشة المراجعة). الهيدر والبحث وشريط التصنيفات وبانر المميز وبطاقات الفئات وترتيب الأقسام والفوتر والشريط السفلي: لم تُلمس. لم تُعدّل نسخة مدير النظام.

### 15. اختبار الإثبات الأخير — الدخول وتبديل الحساب وعزل الإدارة (2026-08-04، جلسات حقيقية)
بيئة الاختبار: `http://localhost:8080` عبر Playwright بجلسات فعلية. حسابات QA فقط (دفعة `qa-switch-2026-08-04-v1`): مستخدم `QA Alpha` (حساب شخصي + منشأة «منشأة اختبار التبديل» بدور owner)، `QA Beta` (شخصي فقط، غير مخوّل للمنشأة)، `QA Admin` (صلاحية `platform_admin` من جدول `mkt_platform_admins`). لم يُعدَّل أي كود واجهة أو لون أو تخطيط في هذه الجولة (اختبار إثبات فقط)، ولم يُنشر شيء.

**15.1 الدخول المباشر**
- زائر يفتح `/dashboard/my-ads` → إعادة توجيه إلى `/auth?next=%2Fdashboard%2Fmy-ads` (بلا Redirect Loop).
- بعد تسجيل الدخول: الوصول المباشر إلى `/dashboard/my-ads` (المسار المطلوب) دون أي مرور على `/choose-account` (`chooseAccountSeen = false`).
- الحساب النشط ظاهر في الهيدر مباشرة باسم «QA Alpha» على 390px و1366px.
- بعد إعادة التحميل: عيّنات نص الهيدر كل 200ms لا تُظهر أي حالة زائر («دخول» / «إنشاء حساب») — الهيدر يبدأ فارغًا ثم يظهر باسم الحساب. لا وميض لحالة زائر.

**15.2 تبديل الحساب (390px و1366px — نتائج متطابقة)**
- قائمة الحساب تُفتح من الهيدر (زر `aria-label="قائمة الحساب"`): Sheet سفلي على الجوال، Dropdown على الكمبيوتر.
- القائمة تعرض الحساب الشخصي + المنشأة المصرّح بها فقط (`menuHasBusiness = true`)، ولا تعرض أي حساب لمستخدم آخر (`menuHasForeign = false`).
- علامة صح واحدة فقط بجانب الحساب النشط (`checkMarks = 1`).
- التبديل إلى المنشأة: بدون إعادة تسجيل دخول، والمستخدم يبقى في الصفحة الحالية (`stayedOnPage = true`)، والهيدر يتحول إلى «منشأة اختبار التبديل»، والمفتاح المحفوظ يصبح `business:8b81b8b2-…`.
- بعد التبديل: 6 طلبات `rest/v1` + `rpc` جديدة (إعلانات/محادثات/تنبيهات/صلاحيات) نتيجة `removeQueries` + `invalidateQueries`، ولا تظهر بيانات الحساب السابق أثناء التحميل (الكاش يُمسح قبل إعادة الجلب).
- العودة إلى الحساب الشخصي: نجحت، الهيدر «QA Alpha» والمفتاح `individual`.

**15.3 عزل الشخصي والمنشأة والأمان**
- `QA Beta` لا يرى المنشأة في قائمة حساباته إطلاقًا (`B_seesBusiness = false`).
- تعديل `localStorage.tahqaq.mkt.account` يدويًا إلى `business:00000000-…-000123` ثم إعادة التحميل: لم يحدث أي تبديل — رجوع تلقائي إلى الحساب الشخصي وتنظيف المفتاح المزيّف (`tamperStored = "individual"`). التحقق خادمي عبر `mkt_account_context` / `mkt_my_accounts` (`SECURITY DEFINER` على `auth.uid()`) و`set_active_tenant`.
- لا اختلاط بين بيانات الحساب الشخصي والمنشأة: مفاتيح الاستعلامات مرتبطة بمعرّف المستخدم والحساب النشط، والكاش يُفرَغ عند كل تبديل.

**15.4 عزل لوحة الإدارة (جلسة `platform_admin` فعلية)**
- زائر يفتح `/admin` → `/auth?next=%2Fadmin`؛ بعد دخول المدير: الوصول الفعلي إلى `/admin`.
- صفحات فرعية فعلية مفتوحة: `/admin/workforce` و`/admin/audit-log`.
- في كل الصفحات: الهيدر الإداري باقٍ («إدارة المنصة») والقائمة الجانبية باقية (`aside` موجود بكل عناصرها)، ولا هيدر سوق عام، ولا بانر «إعلانات مميزة»، ولا شريط تصنيفات السوق، ولا فوتر السوق، ولا شريط سفلي عام (`marketBottomBar = false`)، وخلفية الإدارة محافظة على قيمها الإدارية دون تسرّب ألوان السوق.
- مستخدم غير مخوّل (`QA Beta`) على `/admin`: «لا تملك صلاحية الدخول — لوحة إدارة السوق متاحة لمخوّلي المنصة فقط» بلا أي بيانات إدارية.

**15.5 الفحوصات التقنية**
- `tsgo --noEmit`: نجح. `vite build`: نجح.
- Console: صفر أخطاء وصفر تحذيرات في كل الجلسات (لا تحذير `Multiple GoTrueClient instances`).
- Network: صفر استجابات ≥400. الطلبات الملغاة الوحيدة (`ERR_ABORTED`) هي استعلامات أُلغيت عند تبديل الحساب — سلوك متوقع.
- عميل واحد فقط: `createClient` مستدعى مرة واحدة في `src/integrations/supabase/client.ts` (بالإضافة إلى العميل الخادمي المنفصل).
- مستمع جلسة واحد: `onAuthStateChange` معرّف في `src/lib/session.tsx` فقط.
- Query Invalidation بعد التبديل: مؤكَّد (إزالة الكاش + إعادة جلب 6 استعلامات).

**15.6 لقطات الإثبات** — `.lovable/audit/2026-08-04-kahli-home/`
`m390_header_personal.png` ، `m390_menu_open.png` ، `m390_header_business.png` ، `d1366_header_personal.png` ، `d1366_menu_open.png` ، `d1366_header_business.png` ، `m_landing_after_login.png` ، `admin_1366_index.png` ، `admin_1366_workforce.png` ، `admin_1366_audit_log.png` ، `nonadmin_1366_admin.png`.

**النتيجة:** جميع الاختبارات نجحت. تم إغلاق مرحلة الهوية الكحلية والرئيسية والدخول وتبديل الحساب وعزل الإدارة بنجاح.

### اللقطات المضافة
`.lovable/audit/2026-08-04-kahli-home/`: `price_ar_1366.png` ، `price_en_1366.png` ، `price_ar_390.png` ، `nofeatured_1366.png` ، `nofeatured_390.png` ، ولقطات الإثبات في 15.6.


### 16. دمج «عقار ديل» في تصنيف «عقارات» (2026-08-04)

**التشخيص:** لا يوجد تصنيف ثانٍ في قاعدة البيانات. `mkt_categories` يحتوي سجلًا واحدًا فقط للعقارات (`real-estate` / «العقارات» / `Real estate`) و11 تصنيفًا فرعيًا. «عقار ديل» كان **شريحة واجهة مكرّرة** في شريط التصنيفات (`mkt-home-nav.ts` → المفتاح `realdeal`) تشير إلى **نفس** التصنيف `real-estate` مع `domain=realestate`. لذلك لا يوجد إعلان أو تصنيف فرعي يحتاج نقلًا، ولا سجل يحتاج حذفًا.

**ما نُفِّذ**
- حُذفت شريحة `realdeal` من `HOME_CATEGORIES` ومفتاحا الترجمة `market.home.cats.realdeal` من `ar.json` و`en.json` — الشريط أصبح 17 شريحة + «المزيد» بلا فراغ وبنفس الترتيب والتصميم واللون والسحب الأفقي.
- `src/lib/mkt-category-alias.ts` جديد: تطبيع (حالة الأحرف، المسافات، الهمزات، الترقيم، الشرطات) + خريطة المسميات القديمة → `real-estate`، ودالة `canonicalCategorySlug` ذات نقطة ثابتة (تمنع Redirect Loop بنيويًا).
- `/categories/$slug`: `beforeLoad` يحوّل أي Slug قديم إلى `real-estate` مع الحفاظ على Query Params والمدينة والفلاتر والترتيب والصفحة وHash (`replace: true`).
- `/search`: `validateSearch` يطبّع `category` إلى المعرّف الأساسي، فلا يظهر خياران عقاريان في البحث أو الفلاتر أو نموذج الإعلان (الجميع يقرأ التصنيفات من `mkt_categories`).
- قاعدة البيانات: جدول `mkt_category_aliases` (قراءة عامة، كتابة للإدارة) + دالة `mkt_norm_label` + Trigger `mkt_categories_no_duplicate` يرفض أي تصنيف رئيسي مكرر بالاسم العربي أو الإنجليزي أو Slug أو Alias بعد التطبيع.
- لا لوحة إدارة تصنيفات في المشروع؛ لم يُلمس الهيدر الإداري ولا القائمة الجانبية ولا ألوان الإدارة، ولا الشريط السفلي، ولا تصميم أي صفحة.

**الأرقام**
- إعلانات نُقلت: **0** (لا سجل تصنيف مكرر؛ 11 إعلانًا عقاريًا منها 8 منشورة بقيت على التصنيف الأساسي دون فقد).
- تصنيفات فرعية دُمجت: **0** — و**11** تصنيفًا فرعيًا عقاريًا محفوظة كما هي.
- مسميات بديلة أصبحت Redirect: **9** سجلات Alias (عقار ديل، عقار دليل، Real Estate Deal، Property deals، aqar-deal، realdeal، real-estate، عقارات، العقارات).
- سجلات مكررة عُطِّلت أو حُذفت: **0** (لم توجد؛ الحُذف الوحيد كان شريحة واجهة ومفتاحي ترجمة).
- تحقق تطبيعي: صفر تصنيفات رئيسية متطابقة بعد التطبيع.

**الاختبارات (Playwright، جلسات فعلية على 320×900 و390×852 و768×1024 و1366×768)**
- «عقار ديل» غائب تمامًا من نص الصفحة في كل المقاسات، و«عقارات» تظهر **مرة واحدة** في الشريط (`aqarat_count = 1`).
- `/categories/real-estate` يعرض إعلانات العقارات (8 بطاقات منشورة) — لم تُفقد أي إعلانات.
- الروابط القديمة: `/categories/real-estate-deal?city=riyadh&sort=price_asc&page=2#list` → `/categories/real-estate?city=riyadh&sort=price_asc&page=2#list` (Params + Hash محفوظة)، `/categories/aqar-deal` → `/categories/real-estate`، `/search?category=real-estate-deal&q=فيلا` → `/search?category=real-estate&q=فيلا&domain=realestate`. الرابط الأساسي لا يعيد التوجيه (لا Loop).
- حماية التكرار: محاولة إدخال تصنيف `aqar_deal` / «عقار ديل» / `Real Estate Deal` رُفضت برسالة «يوجد تصنيف مطابق أو مسمى بديل مرتبط بتصنيف عقارات.»
- صفر Console Errors وصفر Overflow أفقي في كل المقاسات. `tsgo --noEmit` و`vite build` ناجحان.
- الكاش: التصنيفات تُقرأ من قاعدة البيانات عبر `loadCategories` (مفتاح واحد `["mkt","categories"]`)، والشريط بيانات ثابتة في الحزمة — فلا تكرار من Cache أو Seed، ولا بيانات Seed تعيد إنشاء «عقار ديل».

**اللقطات:** `.lovable/audit/2026-08-04-kahli-home/`: `home_320.png` ، `home_390.png` ، `home_768.png` ، `home_1366.png` ، `realestate_1366.png`.
