# الإغلاق التشغيلي النهائي — تحقّق (2026-08-01)

## 1) إصلاح `active_tenant_id` غير الصالح — ✅
- 27 ملفًا شخصيًا كان `active_tenant_id` فيها يشير إلى كيان مؤرشف → صُفِّرت (`NULL`) عبر Migration.
- تحقق بعدي: `bad_profiles = 0`، `null_active = 46` (تُوجَّه لشاشة اختيار الحساب طبيعيًا).
- 27 سجلًا في `audit_log` بالإجراء `repair_active_tenant`.

## 2) كلمة المرور — اختبار حي بحساب مسؤول مؤقت ✅
| # | الاختبار | النتيجة |
|---|---|---|
|1|دخول بكلمة مرور مؤقتة|✅ 200|
|2|`must_change_password = true` عند أول دخول|✅|
|3|كلمة ضعيفة (`abc12345`) — سياسة التطبيق + Auth|✅ مرفوضة (422 weak_password)|
|4|كلمات مسرّبة (Sunshine123! / Football2019! / Baseball#2020)|✅ الثلاث مرفوضة بـ HIBP|
|5|كلمة قوية|✅ مقبولة|
|6|الدور والعضوية بعد التغيير|✅ accountant + نفس الكيان|
|7|تسجيل الخروج|✅ 204، و**refresh token مُبطَل** (400)|
|8|القديمة مرفوضة / الجديدة تعمل|✅ / ✅|
|9|كلمة المرور غير مخزّنة في `audit_log` ولا سجل المستخدم|✅|
|10|توكن الوصول القديم|⚠️ يبقى صالحًا حتى انتهاء صلاحيته (~ساعة) — سلوك JWT قياسي؛ التجديد مُبطَل|

### «نسيت كلمة المرور»
`recover` يرد 200 لبريد موجود وغير موجود بنفس الجسم (لا كشف حسابات) • الرابط يعمل مرة واحدة (إعادة الاستخدام → `otp_expired`) • رابط غير صالح مرفوض • كلمة ضعيفة مرفوضة داخل مسار الاستعادة (422) • الدخول بالجديدة ✅ • الأدوار والعضويات دون تغيير ✅.

## 3) اختبار الأدوار بتوكنات فعلية — 25/25 ✅
مسؤول + موظف + مشرفان (أحدهما بصلاحية `custody.view_own`) داخل كيان اختبار حقيقي:
- المسؤول يرى الأسطح الإدارية؛ الموظف والمشرف ليسا محاسبَين (لوحة الإدارة محجوبة).
- الطلب العام خاص بمقدّمه (1/0)؛ طلب المشروع لا يظهر للمشرف الآخر.
- الرسائل: المشتركة تظهر للطرفين، `requester_only` محجوبة عن المشرف الآخر، الملاحظات الداخلية محجوبة عن المشرفين.
- التذكير يبقى على نفس `request_id`؛ دورة `needs_info → رد المشرف → supervisor_replied` تعمل والمحاسب يرى الرد.
- العهدة: مشرف بلا صلاحية = 0 صف (وحتى بالرابط المباشر)؛ مشرف بصلاحية يرى عهدته فقط ولا يرى عهدة غيره.
- الفواتير مقيّدة بسجل المشرف نفسه (sup1=1، sup2=0) والمرفقات ضمن النطاق المسموح فقط.
- تلفيق المعرفات لا يتجاوز RLS؛ سحب عضوية المشروع يُلغي الوصول فورًا؛ الجلسة والسياق يصمدان عند التحديث؛ الكيان المؤرشف لا يُفعَّل (`NO_ACTIVE_MEMBERSHIP`).

## 4) الوظائف التشغيلية — ✅
إنشاء مشروع بمحاسب ✅ • فاتورة جديدة ✅ ومنع التكرار (23505 على `invoices_duplicate…`) ✅ • دورة عهدة كاملة: الطلب لا يحرّك الرصيد، الاعتماد لا يحرّكه، التنفيذ يُنشئ **حركة واحدة** بمبلغ صحيح، وإعادة التنفيذ مرفوضة بلا تكرار ✅ • رفع مستند لسلة خاصة + رابط موقّع قصير للمحاسب ✅ ورفضه لمشرف غير مصرّح ✅ ولا قراءة عامة ✅.

### 🐞 خلل حقيقي كُشف وأُصلح
`request_messages` كان بلا أي سياسة INSERT/UPDATE → **كل إرسال رسالة يفشل بـ 42501 لكل الأدوار** (المحادثة معطّلة فعليًا في الإنتاج). أُضيفت:
- `request_messages_insert_scoped`: الكاتب = `auth.uid()` + وصول للطلب + الموظفون فقط يختارون الرؤية، ومقدّم الطلب مقيّد بـ `shared`.
- `request_messages_update_own`: تعديل رسائله فقط.
أُعيد الاختبار: إرسال المشرف 201 والمحاسب يقرأ الرسالة ✅.

## 5) نسخة Auth المُدارة — ✅ مع حد معروف
جرد مستخدمي Auth قابل للتصدير عبر Admin API (60 مستخدمًا، لقطة في `/mnt/documents/backup/auth-users-count.txt`). مخططا `auth` و`storage` مملوكان للمنصة ولا يمكن تفريغهما؛ خطة إعادة الربط موثقة في `.lovable/backup/2026-08-01-restore-point.md`.

## 6) الفحص النهائي
- `tsgo --noEmit` ✅ و`vite build` ✅ (بناء Nitro مكتمل).
- التسجيل العام مغلق، HIBP مفعّلة، لا أسرار في حزمة العميل.
- Linter: 108 نتيجة (1 INFO لجدول `rate_events` المغلق + WARN لدوال RPC متاحة للمسجّلين بحكم التصميم) — **Critical/Error = 0**.
- بيانات الاختبار: حساباتها الأربعة حُذفت من Auth، والكيان أُرشف (`archived` + `deleted_at`) وطلباته حُذفت منطقيًا. حركات العهدة المعتمدة بقيت لأن الحذف/التعديل عليها ممنوع بالتصميم (تصحيح بحركة عكسية فقط)، وهي داخل كيان مؤرشف بلا مستخدمين.

## الحالة النهائية
**جاهز للنشر** — كل بنود الإغلاق التشغيلي منفَّذة ومختبَرة حيًّا، مع خللٍ واحد كُشف وأُصلح (سياسات كتابة المحادثة).
حدود معروفة: توكن الوصول القديم يبقى صالحًا حتى انتهاء صلاحيته، ونسخ `auth`/`storage` تعتمد على المنصة.

---

## 7) التثبيت النهائي (Baseline) — 2026-08-02

| البند | الحالة |
|---|---|
| ترحيل إصلاح الـ27 ملفًا | ✅ محفوظ في `supabase/migrations/20260801160448_d48b0879-3f8a-4e70-b0b7-5cc525c461dc.sql` (`repair_active_tenant`) |
| سياسات `request_messages` (insert/update) | ✅ محفوظة في `supabase/migrations/20260801161049_db719bf2-bf1d-440f-9afb-020f18e0ab8f.sql` — ليست يدوية |
| TypeScript (`tsgo --noEmit`) | ✅ 0 أخطاء |
| البناء (`vite build` + Nitro) | ✅ ناجح |
| Linter قاعدة البيانات | 108 نتيجة (INFO/WARN بحكم التصميم) — **Critical/Error = 0** |
| تغييرات غير محفوظة في الملفات | ✅ لا شيء عدا هذا التقرير |
| Commit / Tag | ⚠️ لا يمكن تنفيذه من داخل البيئة — حالة Git تُدار بواسطة المنصة. الرسالة المعتمدة: `chore: complete operational closure and fix request messages policies` والوسم المطلوب: `operational-closure-v1.0` — يُنشآن من واجهة GitHub/المنصة. |

**الخلاصة:** هذه النسخة معتمدة كـ **Baseline مستقرة** للمرحلة التالية؛ لا تعديلات على الواجهات أو الصلاحيات أو القاعدة بعد هذه النقطة إلا بطلب صريح.
