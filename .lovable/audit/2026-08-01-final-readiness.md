# تقرير الجاهزية النهائية — تحقّق (2026-08-01)

## 1) النسخة الاحتياطية — ✅
انظر `.lovable/backup/2026-08-01-restore-point.md`.
- الملف القديم `2026-08-01-pre-registration-lockdown.md` كان **تقريرًا توثيقيًا فقط**، لا نسخة.
- النسخة الحقيقية: `tahqaq-2026-08-01T152432Z` (Schema + Data + RLS + Functions + Triggers + Storage metadata + الملفات الفعلية 5/5).
- اختبار استعادة كامل في بيئة معزولة: بيانات 0 فشل، أعداد الصفوف مطابقة 100%، 136 FK بلا مخالفات، 0 صف يتيم.
- حدود معروفة: `auth` و`storage` مملوكان للمنصة ولا يمكن تفريغهما — خطة إعادة الربط موثقة.

## 2) الكيانات المؤرشفة — ✅ مع ملاحظة
- 84 كيانًا مؤرشفًا لا تظهر في `my_accounts` / `my_tenants` / `my_workspace_state` (فلترة `status='active' AND deleted_at IS NULL`).
- `activate_account` ترفض التبديل إليها، ومشغّلا `freeze_tenant_id` (2) يمنعان نقل السجلات بين الكيانات.
- ⚠️ 27 ملفًا شخصيًا لا يزال `active_tenant_id` فيه يشير إلى كيان مؤرشف. لا يكسر العزل (الدوال تفلتر)، لكنه قد يعطي شاشة اختيار حساب فارغة لهؤلاء. إصلاح مقترح: تصفير `active_tenant_id` لهم.

## 3) الفحص الأمني النهائي — ✅
| البند | النتيجة |
|---|---|
| التسجيل العام | مغلق — `POST /auth/v1/signup` يرد `422 signup_disabled` |
| الحسابات المجهولة | معطّلة (`anonymous_users: false`) |
| HIBP | مُفعّلة (Auth) + سياسة تطبيقية موحدة `src/lib/password-policy.ts` (≥10 أحرف + تعقيد + منع الشائع) |
| RLS | مُفعّلة على كل جداول `public` (0 جدول بدون RLS) |
| جدول بلا سياسات | 1 فقط (`rate_events` — مغلق تمامًا بلا وصول) |
| SECURITY DEFINER | 111 دالة، **كلها** بـ `search_path` ثابت؛ المتاح لـ anon = 2 فقط (معاينة الدعوة + التحقق من الفاتورة) |
| `ensure_personal_tenant` | غير قابلة للتنفيذ من anon ولا authenticated |
| تحديد المعدل | `rate_limit_hit` غير قابلة للتنفيذ من أدوار العميل (تُستدعى من الخادم فقط) |
| أسرار في الواجهة | 0 — لا `service_role` ولا `sb_secret_` في `dist/client` |
| BUILD / TypeScript | `vite build` ✅ ، `tsgo --noEmit` ✅ (0 خطأ) |
| فحص الأمان | 107 نتيجة **كلها warn** (دوال RPC متاحة للمستخدم المسجّل بحكم التصميم) — **Critical = 0، Error = 0** |

## 4) بنود لم تُنفَّذ في هذه الجولة — ⛔
- اختبار مسار تغيير كلمة المرور الإلزامي و«نسيت كلمة المرور» بحساب مسؤول اختبار حقيقي (تحقّقنا من الكود والسياسة، لا من التنفيذ الحي).
- 25 سيناريو أدوار بتوكنات فعلية (مسؤول/موظف/مشرف بعهدة وبدونها). العزل تحقّقنا منه على مستوى RLS والدوال والاستعلامات، لا عبر جلسات مستخدمين حقيقية.

## الحالة النهائية
**جاهز تقنيًا وأمنيًا للنشر** (نسخة استعادة مثبتة، تسجيل مغلق، HIBP مُفعّلة، Critical/Error = 0، بناء ناجح)،
مع تحفّظين: 27 ملفًا شخصيًا بحاجة تصفير `active_tenant_id`، والاختبارات الحيّة للأدوار وكلمة المرور لم تُنفَّذ بعد.
