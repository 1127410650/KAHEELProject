# توحيد «المجالات الرئيسية» (19 عنصرًا) — 2026-08-05

## المصدر الوحيد
- `src/lib/market-primary-navigation.ts`: القائمة المعتمدة بترتيبها الحرفي مع معرّفات ثابتة، دوال
  `fieldSearchParams` / `isFieldActive` / `rootSlugRank`، وحاجز تفرّد عند التحميل.
- الأسماء العربية/الإنجليزية في `src/i18n/ar.json` و `en.json` تحت `market.fields.*`.
- حُذف الإعداد القديم `src/lib/mkt-home-nav.ts` بعد التأكد من عدم وجود أي مستهلك.

## الواجهات المرتبطة بالمصدر
| الواجهة | الحالة |
| --- | --- |
| شريط الرئيسية `MarketCategoryStrip` | صف واحد قابل للتمرير، «الرئيسية» → `/`، «المزيد» → `/more` |
| صفحة البحث `/search` | الشريط نفسه أعلى الصفحة + تفعيل المجال الحالي |
| صفحة `/more` | شبكة «المجالات» من القائمة المركزية (بدون الرئيسية/المزيد) |
| نافذة «اختر المجال» في نموذج الإعلان | ترتيب الجذور = الترتيب المعتمد عبر `rootSlugRank` |

## «عقار ديل»
مجال واحد فقط في الواجهة: يفتح `/search?category=real-estate&sub=re-aqar-deal`.
لا يوجد جذر مكرر في قاعدة البيانات، و`sub` يُحلّ من الـ slug إلى المعرّف داخل `search.tsx`
لذا تعمل الروابط القديمة (المعرّف) والجديدة (الـ slug) معًا.

## البحث في نافذة المجال
البحث في المستوى الأعلى يمسح الشجرة كاملة (عربي/إنجليزي/slug) بتطبيع الهمزة والتاء المربوطة
والمسافات، ويعرض المسار الكامل «خدمات ← ...»؛ الاختيار ينتقل للمستوى الفرعي أو يُنهي الاختيار.

## الإثبات
- 7 مقاسات (320 / 390 / 430 / 768 / 1024 / 1366 / 1440): الشريط صف واحد، ارتفاع العنصر ≥ 44px،
  لا تمرير أفقي للصفحة، لا أخطاء Console.
- تطابق القائمة حرفيًا بالعربية والإنجليزية على كل المقاسات، وعلى `/search` و`/more`.
- جذور قاعدة البيانات مطابقة للترتيب المعتمد (استعلام تحقق).
- `tsgo --noEmit` نظيف.
- لقطات: `/tmp/browser/fields/shots/rail_320.png`, `rail_390.png`, `rail_1366.png`.

---

## مجال «مدارس وجامعات» (سجل حقيقي في قاعدة البيانات)

| البند | القيمة |
| --- | --- |
| المعرّف | `b7c1f2a4-9e3d-4c58-8a71-2d6f5b0c1e94` |
| Slug | `schools-universities` |
| الاسم | مدارس وجامعات / Schools & Universities |
| الترتيب | `sort_order = 11` — بعد «تدريب» (10) مباشرة، وأُزيح ما بعده (+1) بلا حذف |
| المعرّف في الواجهة | `id: "schools"` داخل `PRIMARY_FIELDS` (المصدر الوحيد) |

«تدريب» بقي مجالًا مستقلًا (`training`, sort 10) ولم يُدمج أو يُنقل.

### التصنيفات الفرعية الحقيقية (`parent_id` = معرّف المجال)
| Slug | الاسم | المعرّف |
| --- | --- | --- |
| edu-schools | مدارس / Schools | `7fb68939-f327-4180-a978-fb707a162a55` |
| edu-universities-colleges | جامعات وكليات | `705e30c9-3b4d-41cb-99cc-70eb7effab86` |
| edu-institutes-academies | معاهد وأكاديميات | `0f7ae47e-acf3-4a49-a1e8-9f58dc6d1c2a` |
| edu-nurseries-kindergartens | حضانات ورياض أطفال | `ffb91273-eb85-4766-9d9f-f6d65e0dc7e9` |
| edu-private-tutoring | دروس خصوصية | `1a54435d-25da-4d4e-a097-f9beaa49c2dd` |
| edu-educational-services | خدمات تعليمية | `40a95558-c355-4781-b239-577a1669dcf7` |
| edu-admission-registration | قبول وتسجيل | `d8513261-6185-437a-ac35-4b3042e0f191` |
| edu-scholarships | منح دراسية | `7cb3175d-c549-49de-8489-3667e4cca466` |
| edu-student-housing | سكن طلابي | `55f623cc-911e-40c9-9faf-b52b061cbbd9` |
| edu-educational-supplies | مستلزمات تعليمية | `da14599a-90d4-44da-bb33-940586fa0425` |
| edu-other | أخرى / Other | `fe395fd8-2233-4039-adc0-168c65882980` |

### Migrations المنفذة
1. فهارس تفرّد للجذور على `mkt_norm_label(slug)` و`name_ar` و`name_en` + إزاحة الترتيب +
   `INSERT ... ON CONFLICT (slug) DO UPDATE` للمجال و11 تصنيفًا فرعيًا (قابلة لإعادة التنفيذ).
2. 17 اسمًا بديلًا في `mkt_category_aliases` (بحث فقط، `alias_norm` مُولّد وفريد): التعليم/تعليم/
   مدارس/مدرسة/جامعات/جامعة/كليات/كلية/معاهد/معهد/أكاديميات/أكاديمية/Education/Schools/
   Universities/Colleges/Institutes — بلا أي سجل تصنيف مكرر.
3. إعلان QA واحد داخل الدفعة `qa-listings-2026-08-04-v1`.

لم يوجد أي سجل سابق بأسماء تعليم/مدارس/جامعات/Education/Schools (تحقق قبل الإنشاء) فلا حاجة لدمج.

### الجداول والاستعلامات
لا قوائم مغلقة: `mkt_listings.category_id` / `subcategory_id` مفاتيح أجنبية إلى `mkt_categories`،
وكل استعلامات الرئيسية والبحث والاقتراحات ونموذج الإعلان وصفحة التصنيف والمميّزة والأحدث تقرأ
التصنيفات من قاعدة البيانات (`mkt-queries.ts`, `search.tsx`, `categories.$slug.tsx`,
`CategoryPicker.tsx`). لم تُعدّل أي سياسة RLS ولا صلاحيات.

### إعلان الاختبار
`a0000000-0000-4000-8000-000000000041` — «إعلان تجريبي: مدرسة أهلية في الرياض»،
`مدارس وجامعات ← مدارس`، `status = published`، `qa_batch_id = qa-listings-2026-08-04-v1`،
يُحذف بـ `public.mkt_qa_cleanup('qa-listings-2026-08-04-v1')`. لا يمثل مدرسة أو جهة حقيقية.
عدد الإعلانات المرتبطة بالمجال حاليًا: 1 (إعلان اختبار فقط).

### نتائج الاختبار
- الظهور بعد «تدريب» في الشريط والبحث و`/more` والقوائم العربية والإنجليزية — عنصر واحد فقط
  (لا تكرار، لا اختلاط مع «تدريب»).
- `/categories/schools-universities`: يعرض الاسم و11 تصنيفًا فرعيًا وإعلان الاختبار فعليًا.
- `/search?q=مدرسة` و`/search?category=schools-universities`: الإعلان يظهر بالمعرّف الحقيقي
  (لا مطابقة اسم فقط)، والمدينة والفلاتر محفوظة.
- 7 مقاسات (320 / 390 / 430 / 768 / 1024 / 1366 / 1440): `scrollWidth = clientWidth` — لا تمرير
  أفقي، الشريط سطر واحد بارتفاعه الأصلي، ولا أخطاء Console (0).
- `/admin` والنظام الداخلي: لا تعديل على التصميم ولا على الهيدر أو القائمة الجانبية أو الألوان؛
  لوحة تصنيفات السوق تقرأ الجذور ديناميكيًا فيظهر المجال تلقائيًا مع عدد إعلاناته الحقيقي.
- `tsgo --noEmit` نظيف و`vite build` ناجح.

### اللقطات
`.lovable/audit/shots/home_390.png`, `home_1366.png`, `category_page.png`,
`search_madrasa.png`, `search_category.png`, `more.png`

### متبقٍ يحتاج جلسة مسجّلة
اختيار المجال داخل نافذة «اختر المجال» والمسودة/الاستعادة/الإرسال/التعديل تحتاج جلسة معلن
(`LOVABLE_BROWSER_AUTH_STATUS = signed_out` حاليًا). المسار جاهز برمجيًا: النافذة تبني الجذور من
قاعدة البيانات بترتيب `rootSlugRank` وتحفظ معرّف آخر تصنيف مختار (`category_id` الجذر +
`subcategory_id`) لا الاسم النصي.
