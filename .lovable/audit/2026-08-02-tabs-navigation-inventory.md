# تقرير شامل: جميع التبويبات وروابط التنقل في المنصة

التاريخ: 02/08/2026 · النطاق: جرد فقط — لم يُحذف أو ينقل أو يُعدّل أي رابط أو مسار أو صلاحية.
المصادر: قراءة الكود الحالي (`MarketShell.tsx`, `AccountMenu.tsx`, `AppLayout.tsx`, `DashboardShell.tsx`, `AdminShell.tsx`, `more.tsx`, `choose-account.tsx`, `select-account.tsx`, `routes-map.ts`) + معاينة حقيقية عبر Playwright على 1366px و 390px.

ملاحظة تنفيذية: حالة المتصفح أثناء الفحص كانت **زائرًا غير مسجّل** (`signed_out`)، لذلك اللقطات تمثّل حالة الزائر، وسطوح الحساب (قائمة الحساب، شاشة اختيار الحساب، القائمة الجانبية التشغيلية) وُثّقت من الكود سطرًا بسطر.

اللقطات: `.lovable/audit/shots/2026-08-02-nav/`
`header_desktop.png` · `header_mobile.png` · `bottomnav.png` · `accountmenu_desktop.png` · `accountmenu_mobile.png` · `more_desktop.png` · `more_mobile.png`

---

## 1. الجدول الكامل

| # | العربية | English | مكان الظهور | المسار | زائر؟ | شخصي؟ | منشأة؟ | إدارة؟ | الصلاحية | مكرر أين؟ | يعمل؟ | ملاحظة | القرار المقترح |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | سوق تحقّق (الشعار) | Brand | هيدر (كمبيوتر+جوال) | `/` | نعم | نعم | نعم | نعم | — | البند 2، 21، 33 | نعم | النص مخفي < sm | إبقاء |
| 2 | السوق | Marketplace | هيدر lg فقط | `/` | نعم | نعم | نعم | نعم | — | الشعار، الشريط السفلي «الرئيسية»، الفوتر، /المزيد | نعم | 4 نسخ لنفس المسار | توحيد |
| 3 | البحث | Search | هيدر (كمبيوتر+جوال) | `/search` | نعم | نعم | نعم | نعم | — | الفوتر، /المزيد، Hero | نعم | حُذف من الشريط السفلي بالتصميم | إبقاء |
| 4 | المزيد | More | هيدر lg + الشريط السفلي + الفوتر | `/more` | نعم | نعم | نعم | نعم | — | 3 نسخ | نعم | — | إبقاء نسخة واحدة لكل سطح |
| 5 | التنبيهات (الجرس) | Notifications bell | هيدر | `/dashboard/notifications` | لا | نعم | نعم | نعم | — | الشريط السفلي، قائمة الحساب، /المزيد | نعم | 4 نسخ | توحيد |
| 6 | تبديل اللغة EN/ع | Language | هيدر (sm+ فقط) | زر | نعم | نعم | نعم | نعم | — | /المزيد (إعدادات)، AppLayout | نعم | **مخفي على 390px** | إظهاره على الجوال |
| 7 | تسجيل الدخول | Sign in | هيدر (sm+ فقط) | `/login` (قديم → `/auth`) | نعم | — | — | — | — | الفوتر، /المزيد | نعم | يستخدم مسارًا قديمًا + مخفي على الجوال | تحويله إلى `/auth` |
| 8 | أضف إعلانًا | Add listing | هيدر + Hero + الشريط السفلي + الفوتر + إعلاناتي | `/dashboard/ads/new` | يُعرض للزائر | نعم | نعم | نعم | — | 5 نسخ | نعم | يظهر للزائر ثم يُحوّل لتسجيل الدخول | إبقاء + توضيح |
| 9 | الرئيسية | Home | الشريط السفلي | `/` | نعم | نعم | نعم | نعم | — | البند 1، 2 | نعم | — | إبقاء |
| 10 | المحادثات | Messages | الشريط السفلي + قائمة الحساب + /المزيد | `/dashboard/messages` | لا (يُعرض) | نعم | نعم | نعم | — | 3 نسخ | نعم | يظهر للزائر في الشريط السفلي | إخفاؤه للزائر |
| 11 | أضف | Add | الشريط السفلي (زر مركزي) | `/dashboard/ads/new` | نعم | نعم | نعم | نعم | — | البند 8 | نعم | — | إبقاء |
| 12 | إعلاناتي | My ads | قائمة الحساب + /المزيد + الفوتر | `/dashboard/my-ads` | لا | نعم | نعم | نعم | — | 3 نسخ | نعم | — | توحيد |
| 13 | طلبات عروض السعر | Requests | قائمة الحساب + /المزيد | `/dashboard/requests` | لا | نعم | نعم | نعم | — | نسختان | نعم | — | إبقاء |
| 14 | المفضلة | Favorites | قائمة الحساب + /المزيد + الفوتر | `/dashboard/favorites` | لا | نعم | نعم | نعم | — | 3 نسخ | نعم | — | توحيد |
| 15 | بلاغاتي | My reports | قائمة الحساب + /المزيد | `/dashboard/reports` | لا | نعم | نعم | نعم | — | نسختان | نعم | — | إبقاء |
| 16 | مخالفاتي | Violations | قائمة الحساب + /المزيد | `/dashboard/violations` | لا | نعم | نعم | نعم | — | نسختان | نعم | — | إبقاء |
| 17 | حسابي الشخصي | Personal profile | قائمة الحساب + /المزيد + الفوتر | `/dashboard/profile` | لا | نعم | نعم | نعم | — | 3 نسخ | نعم | الفوتر يعرضه للزائر | إخفاؤه للزائر في الفوتر |
| 18 | ملف المنشأة | Business profile | قائمة الحساب (منشأة فقط) + /المزيد + الفوتر | `/dashboard/business` | لا | **لا** | نعم | نعم | — | 3 نسخ | نعم | **الفوتر و/المزيد يظهرانه للحساب الشخصي/الزائر** | تصفية بنوع الهوية |
| 19 | إضافة منشأة | Add business | قائمة الحساب + /المزيد + شاشة الحسابات | `/choose-account` | لا | نعم | نعم | نعم | — | 3 نسخ | نعم | نفس المسار مثل «تغيير الحساب» باسمين مختلفين | فصل أو تسمية واحدة |
| 20 | تغيير الحساب | Change account | قائمة الحساب + /المزيد | `/choose-account` | لا | نعم | نعم | نعم | — | نسختان + البند 19 | نعم | اسمان لمسار واحد | توحيد |
| 21 | الإعدادات | Settings | قائمة الحساب | `/more` | لا | نعم | نعم | نعم | — | البند 4 | نعم | **الاسم «الإعدادات» يفتح صفحة «المزيد»** | إعادة تسمية أو مسار إعدادات فعلي |
| 22 | تسجيل الخروج | Sign out | قائمة الحساب + شاشة اختيار الحساب + القائمة الجانبية | زر | لا | نعم | نعم | نعم | — | 3 نسخ | نعم | — | إبقاء |
| 23 | إعلانات السوق (إدارة) | Admin listings | تبويبات AdminShell + /admin | `/admin/listings` | لا | — | — | نعم | admin/staff | نسختان | نعم | — | إبقاء |
| 24 | طلبات التوثيق | Verifications | تبويبات AdminShell + /admin | `/admin/verifications` | لا | — | — | نعم | admin/staff | نسختان | نعم | — | إبقاء |
| 25 | الجغرافيا | Geo | تبويبات AdminShell | `/admin/geo` | لا | — | — | نعم | admin | — | نعم | — | إبقاء |
| 26 | البلاغات (إدارة) | Admin reports | تبويبات AdminShell | `/admin/reports` | لا | — | — | نعم | admin/staff | — | نعم | — | إبقاء |
| 27 | لوحة التحكم | Dashboard | القائمة الجانبية التشغيلية | `/dashboard` | لا | لا | — | — | — | — | نعم | كيان تشغيلي فقط | إبقاء |
| 28 | المشرفون / المشاريع / العهدة / الطلبات / الموردون / الفواتير / المنتجات / التقارير | Operations group | القائمة الجانبية (AppLayout) | `/supervisors` `/projects` `/custody` `/requests` `/suppliers` `/invoices` `/products` `/reports` | لا | حسب الدور | — | — | صلاحيات لكل عنصر | — | نعم | معزولة بالصلاحيات | إبقاء |
| 29 | المستخدمون / الفريق / السلة / سجل العمليات / الإعدادات | System group | القائمة الجانبية | `/users` `/team` `/trash` `/audit` `/settings` | لا | جزئي | — | — | accountant | — | نعم | — | إبقاء |
| 30 | بوابة المشرف (الرئيسية/طلباتي/مشاريعي/عهدتي/التنبيهات/إعداداتي) | Supervisor portal | القائمة الجانبية (نسخة المشرف) | `/portal` `/requests` `/projects` `/my-custody` `/notifications` `/settings` | لا | — | — | — | custody.view_own | — | نعم | مطابق لقاعدة الذاكرة | إبقاء |
| 31 | الحساب الشخصي (الرئيسية/طلباتي/مشاريعي/مستنداتي/عهدتي/الدعوات/التنبيهات/إعداداتي) | Personal sidebar | القائمة الجانبية (نسخة شخصية) | `/me` `/requests` `/projects` `/my-documents` `/my-custody` `/invitations` `/notifications` `/settings` | لا | نعم | — | — | — | `/notifications` مقابل `/dashboard/notifications` | نعم | **نظامان للتنبيهات** | توحيد لاحقًا |
| 32 | اختيار الحساب (دخول/إضافة منشأة/تسجيل خروج) | Choose account | `/choose-account` + `/select-account` | — | لا | نعم | نعم | نعم | — | شاشتان لنفس الغرض | نعم | `/select-account` (تشغيلي) و `/choose-account` (سوق) متعايشتان | فصل واضح أو دمج |
| 33 | الفوتر: تصفح / الحساب / الأعمال (9 روابط) | Footer groups | الفوتر (sm+ فقط) | `/`, `/search`, `/more`, `/login`, `/dashboard/profile`, `/dashboard/favorites`, `/dashboard/ads/new`, `/dashboard/my-ads`, `/dashboard/business` | كلها للزائر | نعم | نعم | نعم | — | كل رابط مكرر في مكان آخر | نعم | **غير مفلتر بالصلاحية/الهوية + مخفي كليًا على الجوال** | تصفية عبر `canSeeLink` |
| 34 | التحقق من فاتورة | Verify invoice | زر داخل `/auth` | `/verify-invoice` | نعم | نعم | نعم | نعم | — | — | نعم | غير مذكور في أي قائمة تنقل | إضافته إلى /المزيد |
| 35 | `/marketplace` | Legacy home | مسار قديم + إعادة توجيه بعد اختيار الحساب | `/marketplace` → `/` | نعم | — | — | — | — | البند 2 | يوجّه | يُستخدم افتراضيًا في `choose-account` | استبداله بـ `/` |
| 36 | `/login` | Legacy auth | هيدر + الفوتر + /المزيد | `/login` → `/auth` | نعم | — | — | — | — | `/auth` | يوجّه | 3 روابط تشير للمسار القديم | تحويلها إلى `/auth` |
| 37 | أزرار داخل النماذج (رجوع/التالي/معاينة/نشر/تغيير الحساب) | Form nav | `ListingForm`, `BusinessQuickCreate`, `NewRequestWizard` | داخلية | لا | نعم | نعم | — | — | — | نعم | أزرار إجراء لا تبويبات | إبقاء |

**تبويبات داخل الصفحات:** لا يوجد أي شريط تبويبات (Tabs) في صفحات السوق أو الحساب أو الإدارة عدا شريط AdminShell — تأكيد أن نقل التبويبات إلى قائمة الحساب اكتمل. لوحة الكيان `/dashboard` تستخدم Accordion لا تبويبات.

---

## 2. تصنيف التبويبات

- **تنقل رئيسي:** 1, 2, 3, 4, 8
- **تنقل جوال:** 9, 10, 11, 5, 4 (الشريط السفلي)
- **قائمة حساب:** 12–22
- **نشاط المستخدم:** 12, 13, 10, 14, 5, 15, 16
- **إدارة الحساب:** 17, 19, 20, 21, 22
- **إدارة المنشأة:** 18
- **إعدادات:** 6, 21, 29 (`/settings`)
- **إدارة المنصة:** 23–26
- **تبويب داخل صفحة:** شريط AdminShell فقط
- **زر إجراء:** 8, 11, 34, 37
- **رابط قديم:** 35 (`/marketplace`), 36 (`/login` ×3)
- **رابط مكرر:** 2, 4, 5, 8, 10, 12, 14, 17, 18, 19/20
- **رابط غير مستخدم:** 34 (`/verify-invoice` بلا مدخل تنقل)، `/admin` (فهرس بلا تبويب)
- **رابط غير مكتمل:** 21 («الإعدادات» → `/more`)
- **رابط يظهر دون صلاحية صحيحة:** 18 و17 في الفوتر (بلا فلترة)، 10 في الشريط السفلي للزائر

---

## 3. كشف التكرار (موقع كل نسخة)

| الرابط | الهيدر | الشريط السفلي | قائمة الحساب | /المزيد | الفوتر | جانبي | إجمالي |
|---|---|---|---|---|---|---|---|
| الرئيسية `/` | ✔ (شعار + «السوق») | ✔ | — | ✔ | ✔ | — | 5 |
| البحث `/search` | ✔ | — | — | ✔ | ✔ | — | 3 |
| المزيد `/more` | ✔ | ✔ | ✔ («الإعدادات») | — | ✔ | — | 4 |
| أضف إعلانًا | ✔ | ✔ | — | ✔ | ✔ | — | 4 (+Hero ×2) |
| المحادثات | — | ✔ | ✔ | ✔ | — | — | 3 |
| التنبيهات | ✔ (جرس) | ✔ | ✔ | ✔ | — | ✔ (`/notifications`) | 5 |
| إعلاناتي | — | — | ✔ | ✔ | ✔ | — | 3 |
| المفضلة | — | — | ✔ | ✔ | ✔ | — | 3 |
| حسابي الشخصي | — | — | ✔ | ✔ | ✔ | — | 3 |
| ملف المنشأة | — | — | ✔ | ✔ | ✔ | — | 3 |
| طلبات عروض السعر | — | — | ✔ | ✔ | — | — | 2 |
| بلاغاتي / مخالفاتي | — | — | ✔ | ✔ | — | — | 2 لكل واحد |
| تغيير الحساب / إضافة منشأة | — | — | ✔✔ | ✔✔ | — | — | 4 (مسار واحد) |
| تسجيل الدخول | ✔ | — | — | ✔ | ✔ | — | 3 (كلها `/login` القديم) |
| تسجيل الخروج | — | — | ✔ | — | — | ✔ + شاشة الحسابات | 3 |
| الإعدادات | — | — | ✔ | ✔ (قسم) | — | ✔ (`/settings`) | 3 (مسارات مختلفة) |

---

## 4. الترتيب الحالي كما يظهر فعليًا

**أ. الهيدر — 1366px** (من اليمين للشمال في RTL):
الشعار «سوق تحقّق» → البحث → (فراغ مرن) → السوق → المزيد → جرس التنبيهات → EN → قائمة الحساب/تسجيل الدخول → أضف إعلانًا.

**ب. الهيدر — 390px:**
أيقونة الشعار (بلا نص) → البحث → (فراغ) → جرس التنبيهات → قائمة الحساب (للمسجّل فقط) → زر «+».
مفقود على الجوال: «السوق»، «المزيد»، زر اللغة، «تسجيل الدخول».

**ج. الشريط السفلي (يمين → شمال):** الرئيسية · المحادثات · أضف (مركزي) · التنبيهات · المزيد.

**د. قائمة الحساب (أعلى → أسفل):**
كتلة الهوية الحالية (صورة + الاسم + النوع + شارة التوثيق) → فاصل → تغيير الحساب → فاصل → **النشاط**: إعلاناتي، طلبات عروض السعر، المحادثات، المفضلة، التنبيهات، بلاغاتي، مخالفاتي → فاصل → **الإدارة**: حسابي الشخصي، ملف المنشأة (للمنشأة فقط)، إضافة منشأة، الإعدادات → تسجيل الخروج.

**هـ. صفحة المزيد:** بطاقة الحساب النشط (تغيير الحساب / إضافة منشأة) → التصفح (السوق، البحث) → البيع (أضف إعلانًا، إعلاناتي، الطلبات، ملف المنشأة) → الحساب (حسابي، المحادثات، التنبيهات، المفضلة) → السلامة (بلاغاتي، مخالفاتي) → الإعدادات (اللغة، تسجيل الدخول للزائر).

**و. تبويبات Dashboard:** لا يوجد شريط تبويبات — `DashboardShell` يعرض العنوان + «تعمل الآن باسم …» فقط، والتنقل من قائمة الحساب.

**ز. تبويبات Admin:** إعلانات السوق → طلبات التوثيق → الجغرافيا → البلاغات (تظهر فقط بعد اجتياز البوابة).

---

## 5. مشاكل التبويبات المسجّلة

1. **تكرار عالي:** «التنبيهات» في 5 أماكن و«الرئيسية» في 5 و«أضف إعلانًا» في 6 (مع Hero).
2. **الفوتر غير مفلتر:** يعرض للزائر «حسابي الشخصي»، «ملف المنشأة»، «المفضلة»، «إعلاناتي» — بلا `canSeeLink`، خلافًا لصفحة /المزيد التي تفلتر.
3. **ملف المنشأة يظهر لهوية غير مسموح لها** (الفوتر) بينما القاعدة `allowed_identity_types: ["business"]`.
4. **«الإعدادات» في قائمة الحساب تفتح `/more`** — اسم لا يطابق الصفحة.
5. **اسمان لمسار واحد:** «تغيير الحساب» و«إضافة منشأة» يفتحان `/choose-account`.
6. **روابط قديمة:** ثلاث نسخ من «تسجيل الدخول» تشير لـ `/login`، و`choose-account` يوجّه بعد الدخول إلى `/marketplace`.
7. **اختلاف جوال/كمبيوتر:** زر اللغة و«تسجيل الدخول» مخفيان تمامًا على 390px؛ الزائر على الجوال لا يجد مدخل دخول في الهيدر.
8. **الفوتر مخفي كليًا على الجوال** (`hidden sm:grid`) فتُفقد 9 روابط.
9. **الشريط السفلي يعرض «المحادثات» و«التنبيهات» للزائر** رغم أنها مسارات حساب.
10. **نظامان للتنبيهات:** `/notifications` (تشغيلي) و`/dashboard/notifications` (سوق) بنفس الاسم المعروض.
11. **شاشتان لاختيار الحساب:** `/choose-account` و`/select-account`.
12. **`/verify-invoice`** بلا مدخل تنقل إلا زر داخل `/auth`.
13. **`/admin`** فهرس بلا تبويب في شريط الإدارة.
14. لا قص نص ولا تمرير أفقي: تم التحقق على 390px و1366px — `scrollWidth == innerWidth` في الرئيسية و/المزيد.
15. جميع التسميات مترجمة عبر `t()`؛ لا توجد نصوص إنجليزية مثبتة في التنقل — عدا العلامات المقصودة «EN / ع» و«Asia/Riyadh · SAR» في الفوتر.
16. خطأ وقت تشغيل مرصود (غير مُعالج بأمرك): حلقة تنقّل في `/auth` → `/select-account` تُنتج `Maximum update depth exceeded` عند غياب الجلسة.

---

## 6. اقتراح ترتيب موحد (مقترح فقط — غير منفّذ)

- **الهيدر (كل المقاسات):** الشعار · البحث · (فراغ) · جرس التنبيهات · اللغة · قائمة الحساب أو تسجيل الدخول · أضف إعلانًا. إظهار اللغة وتسجيل الدخول على الجوال أيضًا.
- **الشريط السفلي:** الرئيسية · البحث · أضف · المحادثات (للمسجّل فقط) · المزيد — ونقل التنبيهات للجرس وحده.
- **قائمة الحساب:** الهوية الحالية · تغيير الحساب · النشاط (إعلاناتي، الطلبات، المحادثات، المفضلة) · السلامة (بلاغاتي، مخالفاتي) · الإدارة (حسابي، ملف المنشأة، إضافة منشأة) · الإعدادات (صفحة إعدادات فعلية) · تسجيل الخروج.
- **/المزيد:** خريطة كاملة واحدة فقط (بما فيها التحقق من فاتورة)، تبقى مفلترة بـ `canSeeLink`.
- **الفوتر:** روابط عامة فقط (السوق، البحث، المزيد، التحقق من فاتورة) بعد تمريرها على `canSeeLink`، وإظهاره على الجوال.
- **مسار واحد لكل غرض:** `/auth` بدل `/login`، `/` بدل `/marketplace`، شاشة اختيار حساب واحدة، نظام تنبيهات واحد.

---

## 7. الوحدة الأولى المنفّذة: الهيدر المشترك (2026-08-02)

### المكونات القديمة الموجودة
- `MarketHeader` داخل `src/components/marketplace/MarketShell.tsx` — المصدر الوحيد فعليًا.
- `DashboardShell` و `AdminShell` لا يملكان هيدرًا خاصًا؛ كلاهما يغلّف `MarketShell` (لذا لا نسخ مكررة للهيدر في السوق).
- `AppLayout` / `PageHeader` تخص التطبيق التشغيلي الداخلي (`/_authenticated/*`) وليست جزءًا من هيدر السوق — لم تُلمس.
- `verify-invoice` و `businesses/$slug` و `u/$username` تستخدم `<header>` كعنوان محتوى داخل الصفحة، لا كهيدر تنقل.

### المكون النهائي المستخدم
`MarketHeader` واحد فقط، بترتيب عناصر متطابق على الكمبيوتر والجوال، والاختلاف بالظهور حسب حالة الجلسة.

### الترتيب قبل / بعد
- قبل (1366px): الشعار → البحث → «السوق» → «المزيد» → الجرس → اللغة → الحساب → أضف إعلانًا.
- قبل (390px): الشعار → البحث → الجرس → الحساب → (+) — دون لغة ودون تسجيل دخول.
- بعد (مسجل): الشعار → البحث → اللغة (كمبيوتر) → التنبيهات → الحساب النشط → أضف إعلانًا.
- بعد (زائر): الشعار → البحث → اللغة (كمبيوتر) → تسجيل الدخول → أضف إعلانًا.

### المحذوف من الهيدر
- رابط «السوق» (الشعار يغطيه).
- رابط «المزيد» (موجود في تنقل الجوال وصفحته).
- الرابط القديم `/login` → استُبدل بالمسار المعتمد `/auth` من `src/lib/routes-map.ts`.
- زر «أضف إعلانًا» للزائر يتحول إلى `/auth?next=/dashboard/ads/new` فيعود بعد الدخول لإنشاء الإعلان.
- تسجيل الدخول لم يبقَ مخفيًا على الجوال (كان `hidden sm:inline-flex`).

### منع التكرار
عنصر واحد فقط من كل نوع: شعار، بحث، لغة، تنبيهات، حساب نشط، زر إضافة. التنبيهات وقائمة الحساب تُركّبان فقط عند وجود جلسة (شرط JSX لا إخفاء CSS).

### نتائج الاختبارات
| الاختبار | النتيجة |
|---|---|
| زائر 320 / 390 / 1366px | 5 عناصر فقط، لا قص، لا overflow |
| العربية RTL / الإنجليزية LTR | `dir=rtl` / `dir=ltr` صحيح، لا خلط لغات |
| أخطاء Console | 0 |
| مسارات الهيدر | `/`, `/search`, `/auth`, `/auth?next=/dashboard/ads/new` — لا `/login` ولا `/marketplace` |
| `tsgo --noEmit` | نجح (0 أخطاء) |
| `vite build` | نجح |
| هيدر مستخدم مسجل / منشأة / عدة منشآت | لم يُلتقط بالمعاينة: حالة الجلسة `signed_out` في بيئة الاختبار؛ منطق الظهور مُتحقق منه في الكود (`session ? bell + AccountMenu : signIn`) واسم الحساب يأتي من `AccountMenu` الموحد المقصوص بصريًا مع بقاء الاسم الكامل داخل القائمة |

اللقطات: `.lovable/audit/shots/2026-08-02-header/` (guest-320, guest-390, guest-1366, guest-en-390).

---

## الوحدة 2 — الشريط السفلي للجوال (مغلقة 2026-08-02)

**المكوّن الوحيد:** `MarketBottomNav` في `src/components/marketplace/MarketShell.tsx` — لا نسخة داخل أي Shell آخر (تم التحقق: `rg "BottomNav"` = تعريف واحد + استخدام واحد).

### الترتيب السابق
الرئيسية `/` · المحادثات `/dashboard/messages` · أضف `/dashboard/ads/new` · التنبيهات `/dashboard/notifications` · المزيد `/more` — **تُعرض كاملة للزائر أيضًا** (محادثات وتنبيهات خاصة ظاهرة بلا جلسة)، وعدّاد الرسائل غير مقيّد بالحساب النشط.

### الترتيب الجديد — الزائر (3 عناصر متساوية)
1. الرئيسية `/`
2. أضف → `/auth?next=%2Fdashboard%2Fads%2Fnew` (يحفظ مسار العودة، بلا `/login`)
3. المزيد `/more`
لا محادثات، لا تنبيهات، ولا أي عدّاد.

### الترتيب الجديد — المستخدم المسجل (5 عناصر، أضف في المنتصف)
الرئيسية `/` · المحادثات `/dashboard/messages` · **أضف** `/dashboard/ads/new` · التنبيهات `/dashboard/notifications` · المزيد `/more`

### مسارات أُزيل استخدامها من الشريط
`/notifications` (الخلاصة التشغيلية)، `/marketplace`، `/login`، ورابط البحث القديم. جميع المسارات تُعرَّف في `BOTTOM_NAV_PATHS` ويُتحقق منها مقابل `routeRuleFor()` من `src/lib/routes-map.ts` في وضع التطوير. لم يُحذف أي مسار من التطبيق.

### الحالة النشطة
دالة واحدة `activeBottomKey(pathname)` تُعيد مفتاحًا واحدًا فقط (أطول تطابق يفوز): `/dashboard/ads*` → أضف، `/dashboard/messages*` → المحادثات، `/dashboard/notifications*` → التنبيهات، `/more*` → المزيد، وغيرها → الرئيسية. `aria-current="page"` على العنصر النشط، ويستحيل تفعيل عنصرين.

### العدّادات
- **المحادثات:** محادثات الحساب النشط فقط (منشأة: `seller_tenant_id`؛ شخصي: محادثات المستخدم بدون tenant)، ثم عدّ `mkt_messages` غير المقروءة التي لم يُرسلها المستخدم. مفتاح الكاش يتضمّن `account_key` فيتغيّر العدّاد عند تبديل الحساب.
- **التنبيهات:** `mkt_notifications` غير المقروءة (RLS: مالكها فقط) — لا خلط مع نظام `/notifications`.
- شارة صفر لا تُعرض إطلاقًا (`badge > 0`)، ولا عدّاد للزائر (الاستعلامات `enabled: !!session`).

### نتائج الاختبارات الفعلية
| الاختبار | النتيجة |
|---|---|
| زائر 320px | 3 عناصر: الرئيسية/أضف/المزيد — لا قص نص |
| زائر 390px | نفس النتيجة |
| عدم ظهور المحادثات للزائر | ✅ |
| عدم ظهور التنبيهات للزائر | ✅ |
| زر أضف للزائر يحفظ العودة | ✅ `/auth?next=%2Fdashboard%2Fads%2Fnew` |
| عدم ظهور الشريط على 1366px | ✅ (`display:none` عبر `lg:hidden`) |
| Safe Area | ✅ `pb-[env(safe-area-inset-bottom)]` + فاصل `h-16 lg:hidden` يمنع تغطية المحتوى |
| overflow أفقي | 0 على 320 و390 و1366 |
| RTL/LTR | `dir=rtl` عربي، `dir=ltr` إنجليزي، لا خلط |
| أخطاء Console | 0 |
| `tsgo --noEmit` | نجح |
| `vite build` | نجح |

### اختبارات مؤجلة — «بانتظار جلسة دخول»
حالة بيئة المعاينة `signed_out`، لذا لم تُنفَّذ فعليًا: مستخدم بحساب شخصي، مستخدم داخل منشأة، ترتيب العناصر الخمسة حيًّا، فتح المحادثات/التنبيهات/المزيد/إنشاء الإعلان، الحالة النشطة لكل مسار خاص، عدّاد المحادثات، عدّاد التنبيهات، وعزل العدّادات حسب الحساب النشط. المنطق مُتحقق منه في الكود فقط.

**اللقطات:** `.lovable/audit/shots/2026-08-02-nav/guest_320.png`، `guest_390.png` (لقطات المستخدم المسجل والمحادثات والتنبيهات مؤجلة لعدم توفر جلسة).

---

## الوحدة 3 — قائمة الحساب الموحدة (مغلقة 2026-08-02)

المكوّن الوحيد: `src/components/marketplace/AccountMenu.tsx`
Dropdown على الكمبيوتر · Bottom Sheet على الجوال · نفس البيانات ونفس الترتيب.

### الترتيب السابق
1. الحساب النشط (مع علامة ✓ مضلّلة) + علامة توثيق تظهر لكل الحالات.
2. تغيير الحساب.
3. نشاطي: إعلاناتي · طلبات عروض السعر · المحادثات · المفضلة · التنبيهات · بلاغاتي · مخالفاتي (بلا عدادات).
4. إدارة: إدارة الحساب الشخصي · إدارة ملف المنشأة · إضافة منشأة · الإعدادات (تشير خطأً إلى `/more`).
5. تسجيل الخروج (داخل مجموعة الإدارة بلا فاصل).

### الترتيب الجديد
1. الحساب النشط: الصورة/الشعار · الاسم · نوع الحساب (حساب شخصي / منشأة) · علامة التوثيق **فقط عند `verification_status = approved`**. لا يُعرض رقم الجوال كاسم (فحص `looksLikePhone`)، والبديل «مستخدم المنصة».
2. تغيير الحساب — رابط واحد فقط، لا تُعرض قائمة الحسابات داخل القائمة.
3. نشاطي: إعلاناتي · المحادثات (عدّاد) · التنبيهات (عدّاد) · المفضلة · بلاغاتي · مخالفاتي.
4. الإدارة:
   - حساب شخصي: إدارة الحساب الشخصي · الإعدادات · إضافة منشأة.
   - منشأة: إدارة ملف المنشأة · أعضاء المنشأة (بالصلاحية فقط) · إدارة الحساب الشخصي · الإعدادات.
   - مدير المنصة: «لوحة الإدارة» هنا فقط (وليس في الهيدر).
5. تسجيل الخروج بعد فاصل.

### روابط حُذفت من القائمة
- «طلبات عروض السعر» (`/dashboard/requests`) — غير مطلوبة في القائمة، تبقى الصفحة والمسار كما هما.
- علامة ✓ المكرّرة بجوار الحساب النشط.
- الإعدادات المشيرة إلى `/more` → أصبحت `/settings` من `routes-map.ts`.
- «إضافة منشأة» أُزيلت من قائمة المنشأة (تُتاح من شاشة الحسابات/الإعدادات فقط).

### الروابط المشروطة بالصلاحية
كل عنصر يمرّ عبر `canSeeLink(path, viewer)` من `src/lib/routes-map.ts` بمعطيات `{signedIn, accountKind, can, isPlatformAdmin}`:
- `/dashboard/business` → منشأة فقط (`allowed_identity_types: ["business"]`).
- `/team` (أعضاء المنشأة) → منشأة + صلاحية `team.view`.
- `/admin` → `mkt_is_platform_admin()` من الخادم.
الإخفاء واجهيّ فقط؛ الحماية الفعلية تبقى في بوابات المسارات و RLS (لم تُغيَّر).

### مسار تغيير الحساب
`hasUnsavedChanges()` (`src/lib/unsaved-changes.ts`) → تحذير عند وجود تغييرات → `clear()` لسياق الحساب → الانتقال إلى `/choose-account`. لا تبديل مباشر داخل القائمة.
قرار الحساب الواحد: يبقى «تغيير الحساب» ظاهرًا دائمًا لأن «إضافة منشأة» متاحة من الشاشة نفسها.

### العدّادات
هوكات مشتركة جديدة `src/lib/mkt-unread.ts` بنفس مفاتيح React Query المستخدمة في الشريط السفلي (طلب واحد لا اثنان):
- المحادثات: محادثات الحساب النشط فقط (`seller_tenant_id` للمنشأة، أو محادثات المستخدم للحساب الشخصي) ثم رسائل غير مقروءة ليست منه.
- التنبيهات: `mkt_notifications` غير المقروءة.
لا شارة عند القيمة 0، ولا عدادات للزائر (القائمة لا تُعرض أصلًا).

### تسجيل الخروج
`clear()` لسياق الحساب (يمسح `tahqaq.mkt.account` ويبطل الكاش) ثم `supabase.auth.signOut()` ثم العودة إلى `/`. المسودات الخادمية لا تُمسّ.

### نتائج الاختبارات الفعلية
- زائر: لا تظهر قائمة الحساب على 320 / 390 / 1366 ✅
- overflow أفقي = 0 على 320 / 390 / 1366 ✅
- أخطاء Console = 0 ✅
- `tsgo --noEmit` ✅ · `vite build` ✅
- RTL عربي ✅ (اللقطات: `/tmp/browser/acct/shots/guest_320|390|1366.png`)

### اختبارات مؤجلة — «بانتظار جلسة دخول»
`LOVABLE_BROWSER_AUTH_STATUS = signed_out`، لذلك لم تُنفَّذ فعليًا: حساب شخصي، حساب منشأة، تعدد المنشآت، تغيير الحساب، فتح كل رابط من القائمة، إخفاء إدارة/أعضاء المنشأة، ظهور لوحة الإدارة، العدّادات وشارة الصفر، تسجيل الخروج ومسح السياق، لقطات 390px للحساب الشخصي والمنشأة ولقطة 1366px للقائمة، وفحص LTR داخل القائمة. تم التحقق منها بمراجعة الكود فقط.

---

## الوحدة 4 — صفحة «المزيد» (مغلقة 2026-08-02)

الملف المتغير: `src/routes/more.tsx` · مفاتيح ترجمة جديدة في `src/i18n/ar.json` و `src/i18n/en.json`
(`market.more.app` · `market.more.accessTitle` · `market.more.usingAccount` · `market.more.pagesSoon`).

### العناصر التي كانت موجودة (قبل)
- التصفح: السوق (`/`) · البحث (`/search`).
- البيع والنشر: إضافة إعلان · إعلاناتي · طلبات عروض الأسعار · ملف المنشأة.
- حسابي: حسابي الشخصي · الرسائل · التنبيهات · المفضلة.
- الأمان والبلاغات: بلاغاتي · مخالفاتي.
- بطاقة هوية فيها «تعمل الآن باسم…» + «تغيير الحساب» + «إضافة منشأة».
- الإعدادات: اللغة + «تسجيل الدخول» يشير إلى المسار القديم `/login`.

### الروابط المكررة التي أزيلت
الرئيسية · البحث · إضافة إعلان · إعلاناتي · طلبات عروض الأسعار · الرسائل · التنبيهات · المفضلة · بلاغاتي · مخالفاتي · حسابي الشخصي · ملف المنشأة · تغيير الحساب · إضافة منشأة.
(الصفحات والمسارات باقية كما هي — أُزيل التكرار من `/more` فقط.)
كذلك أُزيل المسار القديم `/login` واستُبدل بـ `/auth` المعتمد.

### الترتيب الجديد
1. سطر هوية نصّي فقط للمسجل: «أنت تستخدم: [اسم الحساب النشط]» — بلا Dropdown ولا تبديل ولا بطاقات منشآت ولا زر إدارة. لا يظهر للزائر.
2. التطبيق: اللغة (مبدّل عربي/إنجليزي كامل داخل الصفحة).
3. الدخول — للزائر فقط: تسجيل الدخول (`/auth`) · إنشاء حساب (`/register`).
4. ملاحظة نصّية بأن صفحات المنصة والسياسات ستُدرَج عند إنشائها.

قائمة مقسّمة بصفوف بسيطة (أيقونة + عنوان + سهم يتبع الاتجاه)، بلا Accordions، بلا فوتر داخل الصفحة، ونهاية الصفحة بمسافة `env(safe-area-inset-bottom)` فوق الشريط السفلي. لا شعار ولا وصف مكرر.

### روابط لم تظهر لعدم وجود صفحة فعلية
لا توجد مسارات مسجّلة في `src/lib/routes-map.ts` ولا ملفات في `src/routes` لـ:
`عن المنصة` · `مركز المساعدة` · `تواصل معنا` · `الشروط والأحكام` · `سياسة الخصوصية` · `سياسة المحتوى`.
لذلك حُذف قسمَا «المنصة» و«السياسات» بالكامل بدل عرض روابط ميتة.
كذلك: «تثبيت التطبيق» غير معروض لعدم وجود PWA manifest في `public/`، و«معلومات الإصدار» غير معروضة لعدم وجود مصدر إصدار في الواجهة.

### الصلاحيات
لا روابط إدارة (`/admin`) ولا تشغيلية (`/projects` · `/invoices` · `/custody` · `/team`) ولا إدارة منشأة/أعضاء داخل `/more`؛ الصفحة أصبحت عامة بالكامل فلا تتيح تجاوز صلاحيات الحساب النشط.

### نتائج الزائر (فعلية)
- 320px / 390px / 1366px: overflow = 0 ✅ · أخطاء Console = 0 ✅
- لا يظهر أي عنصر ممنوع (فحص نصّي لـ 12 عنصرًا: الرئيسية، المحادثات، التنبيهات، إضافة إعلان، تغيير الحساب، إعلاناتي، المفضلة، بلاغاتي، مخالفاتي، إدارة الحساب، ملف المنشأة، تسجيل الخروج) ✅
- تسجيل الدخول + إنشاء حساب يظهران للزائر فقط ✅
- تغيير اللغة يعمل: عربي `dir=rtl` ✅ · إنجليزي `dir=ltr` بلا أي كلمات عربية (عدا اسم زر التبديل «العربية») ✅
- `tsgo --noEmit` ✅ · `vite build` ✅
- لقطات: `/tmp/browser/acct/shots/more_guest_ar_320|390|1366.png` و `more_guest_en_390.png`

### اختبارات مؤجلة — «بانتظار جلسة دخول»
`LOVABLE_BROWSER_AUTH_STATUS = signed_out`: لم تُنفَّذ فعليًا صفحة المزيد للمستخدم المسجل (ظهور سطر «أنت تستخدم…»، غياب تسجيل الدخول/الخروج، لقطة 390px للمسجل). تم التحقق منها بمراجعة الكود فقط.
- اختبارات فتح صفحات عن المنصة/المساعدة/التواصل/الشروط/الخصوصية غير قابلة للتنفيذ: الصفحات غير موجودة بعد.

## الوحدة 5 — الصفحة الرئيسية `/` (مغلقة 2026-08-02)

### الترتيب قبل التعديل
هيدر → قسم رئيسي → شريط المجالات → إعلانات مقترحة → أحدث الإعلانات → معدات للتأجير → خدمات ومقاولات → منتجات ومواد بناء → فوتر → شريط سفلي.

### الترتيب بعد التعديل
1. الهيدر المشترك (بلا تغيير) 2. القسم الرئيسي المختصر (العنوان + زر «أضف إعلانًا» فقط) 3. المجالات الخمسة 4. «إعلانات مقترحة لك» عند وجود نتائج 5. «أحدث الإعلانات» عند وجود نتائج 6. الحالة الفارغة عند عدم وجود إعلانات 7. الفوتر العام 8. الشريط السفلي (جوال).

### الأقسام المحذوفة أو المدمجة
- حُذفت ثلاثة أقسام مكررة: «معدات للتأجير»، «خدمات ومقاولات»، «منتجات ومواد بناء» (وصولها الآن من شريط المجالات و`/search`).
- حُذفت استعلاماتها الثلاثة → الصفحة تنفّذ استعلامين مجمعين فقط (أحدث + مقترح) بدل خمسة.
- لا مربع بحث كبير، لا محدد دولة/مدينة، لا مبدّل حساب، زر إضافة واحد في القسم الرئيسي (والثاني داخل الحالة الفارغة فقط).

### المجالات الخمسة ومساراتها
عقارات `/search?category=real-estate` · خدمات `/search?type=service` · منتجات `/search?type=product` · معدات للإيجار `/search?type=equipment_rent` · موردون أو منشآت `/search?advertiser=business`.
لا تصنيفات فرعية على الرئيسية. جوال: شبكة 2+2+1 (العنصر الأخير بعرض كامل) بلا تمرير أفقي؛ كمبيوتر: صف واحد متوازن.

### منطق إخفاء الأقسام الفارغة
`ListingSection` يُرجع `null` عند `items.length === 0` بعد انتهاء التحميل، فلا يظهر عنوان ولا رابط «عرض الكل» لقسم فارغ. Skeleton أثناء التحميل فقط.
منع التكرار: يُستبعد أي إعلان ظهر في «المقترحة» من «الأحدث» (`suggestedIds`).

### النتائج الفعلية
- لا توجد إعلانات منشورة في البيانات الحالية → ظهرت الحالة الفارغة: «لا توجد إعلانات منشورة حاليًا» + زر «أضف إعلانًا»، ثم الفوتر بمسافة طبيعية بلا فراغ ضخم (ارتفاع الصفحة 763px على 390px).
- الاقتراحات: منطق النشاط + fallback (مدينة الحساب ← دولة الحساب) قائم كما هو؛ لم يعتمد التوثيق في الترتيب.

### نتائج 320 / 390 / 1366
overflow = 0 في الأحجام الثلاثة، لا أخطاء Console، العنوان الصحيح ظاهر، لا مربع بحث داخل الصفحة، المجالات الخمسة فقط.
العربية RTL كاملة؛ الإنجليزية LTR مع الترجمة: "Type everything you need here: real estate, services, suppliers, equipment and building materials".
`tsgo --noEmit` ✅ · `vite build` ✅.

### اختبارات مؤجلة
- الصفحة الرئيسية للمستخدم المسجل: بانتظار جلسة دخول.
- بطاقات الإعلانات (صورة/سعر/مدينة/تصنيف فرعي/وقت/مفضلة) و«عرض الكل» وعدم تكرار الإعلان: بانتظار إعلانات منشورة حقيقية (لم تُنشأ بيانات تجريبية).
- تم التحقق كودًا من عدم عرض علامة التوثيق على بطاقة الإعلان (`VerifiedBadge` غير مستدعى في `ListingCard`).

### الملفات المتغيرة
- `src/components/marketplace/MarketHome.tsx`

### اللقطات
`/tmp/browser/home/screenshots/ar_320.png` · `ar_390.png` · `ar_1366.png` · `en_390.png` (وكلها تمثل حالة عدم وجود إعلانات).

---

## الوحدة السادسة — مراجعة وترتيب صفحة البحث `/search` (2026-08-02)

### البنية النهائية للصفحة (بالترتيب)
هيدر ← عنوان الصفحة (H1 «البحث في السوق») ← حقل بحث واحد (Debounce 450ms + Enter) ← صف أدوات: زر «تصفية» + قائمة الترتيب + مبدّل العرض (شبكة/قائمة) + عدد النتائج ← النتائج (Infinite Scroll) ← حالة فراغ ← شريط سفلي (جوال). لا فوتر داخل صفحة البحث.

### حقل البحث
- حقل واحد فقط، بلا تكرار؛ بحث الهيدر يوجّه إلى `/search?q=`.
- Debounce 450ms، Enter يطبّق فورًا، الكلمة محفوظة في الـURL وتظل ظاهرة بعد التحديث والرجوع.

### التصفية (زر واحد)
- Bottom Sheet على الجوال، Sheet جانبي على الكمبيوتر، وتُطبَّق بزر «تطبيق» + «مسح الفلاتر».
- الحقول: المجال الرئيسي (المجالات الخمسة)، التصنيف (يُعطَّل ويُفرَّغ عند عدم توافقه مع المجال)، المدينة، نوع العرض، نطاق السعر، «إعلانات تحتوي على صور».
- في مجال «موردون ومنشآت» تختفي حقول التصنيف/السعر/الصور تلقائيًا.

### الترتيب
الأحدث · الأقدم · السعر تنازلي/تصاعدي (وفي وضع المنشآت: الأحدث/الأقدم فقط).

### العرض والنتائج
- مبدّل شبكة/قائمة مع حفظ التفضيل في `localStorage` (`tahqaq.mkt.search.view`) — تحقّق: بقي «قائمة» بعد إعادة التحميل (`aria-pressed=true`).
- 20 نتيجة/دفعة، Infinite Scroll مع Skeleton، ومنع التكرار بمعرّف فريد.
- حفظ موضع التمرير في `sessionStorage` واستعادته عند الرجوع.
- عند مجال «موردون ومنشآت» تُعرض بطاقة المنشأة (الشعار، الاسم، المدينة، علامة التوثيق إن وُجدت) — ظهرت نتيجة واحدة مطابقة للبيانات الفعلية.

### عزل الدولة
لا يوجد اختيار دولة في الواجهة؛ الاستعلامات مقيّدة بدولة الحساب النشط (السعودية افتراضيًا للزائر)، والمدن تُحمَّل داخل تلك الدولة فقط.

### إصلاحات ظهرت في الاختبار
- تراكب زر إغلاق الورقة مع العنوان → `pe-12` في رأس الورقة.
- مربع اختيار «إعلانات تحتوي على صور» كان يتمدد إلى 40px بسبب قاعدة `button { min-height: 40px }` العامة → استُثني `role="checkbox"`/`role="switch"` في `src/styles.css` (القياس الآن 16×16).
- توافق الروابط القديمة: `advertiser=business` → `domain=business`، `category=real-estate` → `domain=realestate`.

### نتائج 320 / 390 / 1366
overflow = 0 في الأحجام الثلاثة (بما في ذلك أثناء فتح ورقة التصفية)، لا أخطاء Console، العربية RTL والإنجليزية LTR («Search the marketplace»).
`tsgo --noEmit` ✅ · `vite build` ✅.

### اختبارات مؤجلة
- تحميل الدفعات (20+20) ومنع التكرار وحفظ موضع التمرير على قائمة طويلة: بانتظار إعلانات منشورة حقيقية (لا توجد حاليًا؛ لم تُنشأ بيانات تجريبية).

### الملفات المتغيرة
- `src/routes/search.tsx` · `src/lib/mkt-queries.ts` · `src/i18n/ar.json` · `src/i18n/en.json` · `src/styles.css`

### إغلاق ملاحظتي نافذة التصفية — `/search` (2026-08-02)

نطاق التعديل: `src/routes/search.tsx` + مفتاح ترجمة واحد (`market.filters.close`) في `ar.json`/`en.json`. لا شيء آخر.

**1. رأس نافذة التصفية**
- Header مستقل داخل `SheetHeader` بـ `flex flex-row items-center justify-between gap-3`؛ العنوان `flex-1 truncate` وزر الإغلاق `shrink-0`.
- أُلغي التموضع المطلق تمامًا: `SheetContent` يُمرَّر له `hideClose` (زر Radix المطلق مُعطَّل)، وزر الإغلاق أصبح `SheetClose` عنصرًا عاديًا في صف الرأس.
- مقاس زر الإغلاق **44×44px** فعليًا (`size-11`) مع `aria-label` و`title` = «إغلاق نافذة التصفية» / "Close filters".
- خط فاصل خفيف: `border-b border-border`.
- جسم النافذة `flex-1 overflow-y-auto` منفصل عن الرأس، فلا تداخل مع أول فلتر.

**2. خيار «إعلانات تحتوي على صور»**
- Checkbox حقيقي مربع (Radix `role="checkbox"`, `id="f-img"`) بمقاس بصري **20×20px** (`size-5 rounded-[4px] border-2`)، داخل حاوية لمس **44×44px**.
- النص في `<label htmlFor="f-img">` بارتفاع لمس `min-h-11`، والصف متوازن في RTL (المربع أولًا ثم النص).
- محدد = علامة صح واضحة على خلفية primary؛ غير محدد = مربع فارغ بحدود.
- CSS العام محايد: `button[role="checkbox"] { min-height: 0 }` موجود مسبقًا في `src/styles.css`، وقواعد `[data-slot="input"] / [data-slot="button"]` لا تنطبق على المربع.

**نتائج الاختبار (قياسات فعلية بـ Playwright)**

| القياس | 320×900 (غير محدد) | 390×852 (محدد) |
|---|---|---|
| زر الإغلاق | 44×44 | 44×44 |
| الفجوة بين الزر والعنوان | 12px (لا تلامس/تغطية) | 12px |
| المربع البصري | 20×20 | 20×20 |
| حالة المربع | `unchecked` (مربع فارغ) | `checked` (علامة صح) |
| «تطبيق» / «مسح الفلاتر» | ظاهران داخل الشاشة | ظاهران |
| Overflow أفقي | لا | لا |
| أخطاء Console | 0 | 0 |
| فتح/إغلاق 3 مرات متتالية | ✅ بلا خلل | ✅ |

- لوحة المفاتيح: حقلا السعر داخل جسم قابل للتمرير مستقل عن الرأس، وأزرار التطبيق/المسح تُمرَّر إليها عند فتح اللوحة.
- RTL وLTR: الرأس والصف يعتمدان `flex` منطقيًا و`text-start`، فينعكسان تلقائيًا.
- `tsgo --noEmit` ✅ · `vite build` ✅
- لقطتا 320px و390px مرفقتان في المحادثة.

---

## صفحة تفاصيل الإعلان — إغلاق بالاختبار البصري الفعلي (2026-08-03)

**بيانات الاختبار (مؤقتة ومعزولة، بلا حقول جديدة وبلا تغيير في RLS)**
- `qa-temp-full-listing`: عنوان طويل، SAR 2,750,000، تصنيف فرعي (العقارات › فلل)، وصف طويل، 8 خصائص، الرياض — حي الياسمين، موقع تقريبي (`location_visibility=approximate`)، 7 صور + صورة غلاف، معلن فرد.
- `qa-temp-empty-listing`: بلا صور، «السعر على الطلب»، حقول فارغة (وصف/حي/إحداثيات)، بلا موقع على الخريطة.
- الإزالة: أُعيد الإعلانان إلى `draft` ثم حُذفا حذفًا مؤقتًا نظاميًا (`deleted_at`) وصُفِّرت `views_count` فلا تدخل الإحصائيات. لم يُحذف أي سجل تدقيق.

**ما تم اختباره فعليًا**

| البند | النتيجة |
|---|---|
| ترتيب البيانات الأساسية قبل الصور (`h1` قبل أول صورة) | ✅ في 320/390/1366 |
| صورة واحدة في كل مرة على الجوال | ✅ (عرض الصورة ≈ 88% من الشاشة، شريط snap) |
| التنقل بين الصور (سحب على الجوال / أسهم على الكمبيوتر) | ✅ بعد إصلاح RTL (كان العدّاد يبقى 1/7) |
| رقم الصورة | ✅ `1 / 7` → `3 / 7` بعد التنقل |
| المصغرات على الكمبيوتر | ✅ 7 مصغرات مع تحديد المصغرة النشطة |
| ملء الشاشة + التكبير + الإغلاق | ✅ الفتح والتكبير والإغلاق بلا بقايا نافذة |
| الوصف القابل للطي | ✅ «عرض المزيد» يعمل |
| إخفاء الخصائص الفارغة | ✅ لا صفوف فارغة، ولا صف سعر مكرر في الإعلان بلا سعر |
| الخريطة التقريبية | ✅ دائرة تقريبية فقط دون دبوس دقيق |
| عدم عرض الإحداثيات كنص | ✅ لا وجود لـ 24.827 / 46.639 في نص الصفحة |
| التوثيق تحت اسم المعلن فقط، وعدم ظهور «غير موثق» | ✅ |
| التواصل للزائر والمستخدم | ✅ «طلب عرض سعر» + «تواصل» + «تسجيل الدخول للتواصل» للزائر |
| أدوات المالك لا تظهر لغيره | ✅ لوحة «إدارة إعلانك» للمالك فقط، ولا لوحة تواصل له |
| المفضلة والمشاركة والبلاغ | ✅ ثلاثة أزرار 44px فوق المعرض |
| حجب غير المنشور عبر الرابط المباشر | ✅ «الإعلان غير متوفر أو تم إزالته» للإعلانين بعد `draft` |
| الإعلان بلا صور | ✅ بديل بصري نظيف «لا توجد صورة» بلا معرض مكسور، وإخفاء الخريطة والوصف |
| Overflow أفقي | 0 على 320 / 390 / 1366، وكذلك في LTR |
| Console | 0 أخطاء |
| RTL / LTR | ✅ `dir=rtl` افتراضيًا و`dir=ltr` بعد التبديل بلا overflow |
| `tsgo --noEmit` · `vite build` | ✅ / ✅ |

**إصلاحات نتجت عن الاختبار**
- `ListingGallery.tsx`: التنقل والعدّاد كانا معطّلين في RTL (قيم `scrollLeft` سالبة) — أصبح الحساب بالقيمة المطلقة والتمرير باتجاه الكتابة.
- `ar.json` / `en.json`: وحدات القياس المفقودة (`sqm`, `years`, `meter`) كانت تظهر بالإنجليزية «Sqm 480» — أصبحت «480 م²».
- `ListingSpecs.tsx`: إزالة صف «السعر على الطلب» المكرر لأنه معروض بجانب العنوان.

**مؤجّل (لعدم توفر جلسة أو صلاحية)**
- مستخدم مسجّل غير مالك: الجلسة المتاحة هي جلسة مالك الإعلان فقط، ولا يمكن إنشاء مستخدم ثانٍ لأن التسجيل بالدعوة فقط.
- حساب منشأة كمعلن: الجلسة الحالية بحسابها الفردي؛ لم يُختبر عرض المعلن كمنشأة بصريًا.
- الإدارة: `mkt_is_platform_admin` = false للجلسة الحالية، فلم تُختبر أدوات المراجع.
- حالة `suspended`: مُنعت نظاميًا («Only platform reviewers can set status suspended») — وهو سلوك أمني صحيح — فتم التحقق من الحجب عبر `draft`.

**اللقطات**: 320/390/1366 لأعلى الإعلان والمعرض وقسم المعلن والموقع وأدوات المالك والإعلان بلا صور (مرفقة في المحادثة).

---

## تحديث 03/08/2026 — تبويب الترخيص + نقل الموقع

### التغييرات
- «بيانات الترخيص العقاري» في النموذج أصبح Accordion مغلقًا افتراضيًا برأس يحتوي أيقونة + العنوان + حالة مختصرة + سهم.
- الحالات المختصرة: غير مكتمل / يحتاج استكمالًا / البيانات مكتملة (علامة صح) / الترخيص منتهي / طلب استثناء قيد المراجعة، وتُحدَّث فورًا بلا Refresh (`licenseSummary` في `src/lib/mkt-license.ts`).
- ملاحظة صريحة أسفل الرأس: اكتمال الحقول ≠ توثيق الترخيص، ولا تظهر كلمة «موثق».
- عند فشل الإرسال: يُفتح التبويب تلقائيًا وينتقل التركيز إلى أول حقل ناقص (`LICENSE_BLOCK_FIELD`).
- صفحة الإعلان: قسم الترخيص أصبح قابلًا للفتح/الإغلاق، مغلق افتراضيًا، ويعرض عند الفتح رقم الترخيص والانتهاء ورخصة النشاط (عند وجودها) وصفة المعلن ورابط التحقق فقط — بلا مستندات أو بيانات خاصة، ولا يظهر لغير العقار.
- حُذفت بطاقة «الموقع» الكبيرة بالكامل، وأصبح الموقع زرًا دائريًا 44×44 بجانب المشاركة في صف: المفضلة — المشاركة — الموقع — البلاغ، مع `aria-label`/`title` = «الذهاب إلى الموقع»، ويُخفى تمامًا عند غياب أي موقع آمن.
- الروابط تفتح Google Maps بصيغة `search/?api=1&query=` (بلا API ولا مفتاح)، دقيق فقط للمصرَّح، ومطموس (~1كم) أو بحث نصي (حي/مدينة/دولة) للتقريبي.

### نتائج الاختبار
- `licenseSummary`: 7/7 حالات ناجحة (فارغ، جزئي، مالك مكتمل، وسيط بلا فال، وسيط مكتمل، منتهي، استثناء).
- إعلان عقاري منشور (`zz-audit-live`) على 320 / 390 / 1366: `overflow = 0`، بلا أخطاء Console، قسم الترخيص مغلق افتراضيًا ويُفتح بالضغط، زر الموقع ظاهر مرة واحدة، لا بطاقة موقع كبيرة، ولا تسرب إحداثيات دقيقة في HTML.
- رابط الموقع للإعلان التقريبي: `https://www.google.com/maps/search/?api=1&query=24.71,46.68` (مطموس، بلا الإحداثيات المخفية).
- `tsgo --noEmit` و `vite build`: ناجحان.
- اللقطات: `/tmp/browser/lic/shots/390_closed.png`، `390_open.png`، `1366.png`.

---

## تحديث 03/08/2026 — ملف المعلن الفردي العام (إغلاق الملاحظات)

### التغييرات
- **ارتفاع الصفحة والفوتر**: في `MarketShell` أصبح `main` يحمل `flex-1` دائمًا (كان مقصورًا على الفوتر الكامل)، داخل حاوية `min-h-dvh` رأسية Flex: الهيدر → `main` → الفوتر → الشريط السفلي الثابت. لا Spacer ولا ارتفاع ثابت ولا `min-height` داخل المحتوى، والمسافة السفلية الوحيدة هي ارتفاع الشريط + `env(safe-area-inset-bottom)`. عند قصر المحتوى يُدفع الفوتر إلى أسفل الشاشة طبيعيًا ولا يبقى فراغ بعده.
- **تنظيف الفوتر**: حُذف `Asia/Riyadh` ورمز العملة من `MarketFooter` و`MarketCompactFooter`؛ يظهر نص الحقوق فقط. لا روابط شروط/خصوصية لعدم وجود صفحات فعلية لها.
- **صياغة عدد الإعلانات**: `activeListingsKey` في `src/lib/mkt-person.ts` + مفاتيح `market.person.count.{zero,one,two,many}`: 0 «لا توجد إعلانات نشطة»، 1 «إعلان نشط»، 2 «إعلانان نشطان»، 3+ «إعلانات نشطة»؛ بالإنجليزية `active listing` / `active listings`. الأرقام غربية (`class="num" dir="ltr"`).

### نتائج الاختبار
- الفوتر: `footerBottom` = ارتفاع النافذة ناقص الشريط السفلي على 320×900 و390×852 و768 و1366 و390×1800 — أي أسفل الشاشة تمامًا وبلا فراغ بعده، والبطاقة الأخيرة غير مغطاة.
- `overflow = 0` في كل المقاسات، وبلا أخطاء Console.
- لا يظهر `Asia/Riyadh` ولا رمز عملة في الفوتر (تحقق نصي مباشر).
- الحالات: ملف بإعلان واحد (`/u/000` → «1 إعلان نشط» / «1 active listing»)، ملف بلا إعلانات (`/u/123` → «لا توجد إعلانات نشطة» + رسالة القائمة الفارغة)، وملف غير متاح.
- التوثيق: الملفان المتاحان `verification_status = none` فلا تظهر علامة ولا عبارة «غير موثق» ولا مساحة محجوزة (`VerifiedBadge` يعيد `null`). لا توجد بيانات ملف موثق في القاعدة حاليًا، فلم تُلتقط لقطة «موثق».
- الجلسات: الزائر يرى «تسجيل الدخول للتواصل» + المشاركة فقط. جلسة المالك غير متاحة في بيئة الاختبار (`LOVABLE_BROWSER_AUTH_STATUS = signed_out`)، فلم يُختبر المالك بصريًا؛ منطق الشرط في `src/routes/u.$username.tsx`: المالك = «تعديل الملف» + المشاركة فقط، والمسجل غير المالك = بيانات التواصل المسموحة + المشاركة، بلا أي زر إداري.
- قائمة النقاط الثلاث في بطاقة الإعلان (`CardMenu`) تعرض المشاركة والبلاغ فقط — لا تعديل ولا إيقاف ولا حذف لأي مستخدم.
- **بلاغ الحساب مؤجل**: `mkt_reports.listing_id` إلزامي، والبلاغ مرتبط بإعلان؛ لن يُوسَّع النظام في هذه الوحدة.
- `tsgo --noEmit` و`vite build`: ناجحان.
- اللقطات: `/tmp/browser/person/screenshots/` — `guest-390.png`، `ar-390.png`، `en-390.png`، `desktop-1366.png`، `noads-390x1800.png`، `guest-390x1800.png`.

## الوحدة التاسعة — ملف المنشأة العام (`/businesses/$slug`)

- **المسار والمصادر**: بقي المسار `/businesses/$slug`، وأُلغيت القراءة المباشرة من `mkt_business_profiles` واستُبدلت بثلاث دوال `SECURITY DEFINER` محدودة الحقول: `mkt_public_business`، `mkt_public_business_categories`، `mkt_public_business_listings` (+ `mkt_business_is_restricted` بدون صلاحية عامة).
- **الخصوصية**: لا تُرجع الدوال `tenant_id` ولا `owner_user_id` ولا السجل التجاري/الرقم الموحد ولا هوية المسؤول ولا مستندات التوثيق ولا الفريق ولا ملاحظات المراجعة. بيانات التواصل العامة تظهر للمسجلين فقط ووفق خيارات العرض.
- **الحالات غير المتاحة**: غير المنشور/الموقوف/المحجوب/المحذوف والكيان الشخصي كلها تعرض «هذا الملف غير متاح» بنفس الشكل دون كشف السبب.
- **الهوية**: شعار أو أحرف أولى، الاسم، التوثيق تحت الاسم فقط (لا مساحة محجوزة لغير الموثق)، النشاط، المدينة، تاريخ الانضمام DD/MM/YYYY، وعدد الإعلانات بصياغة عربية سليمة.
- **الأزرار**: العضو المخوّل يرى «إدارة الملف»؛ الأعضاء لا يرون أزرار التواصل مع أنفسهم؛ الزائر يرى تسجيل الدخول للتواصل + مشاركة (Web Share API مع نسخ الرابط) + زر موقع دائري يفتح Google Maps ببحث نصي (مدينة/منطقة) دون أي إحداثيات.
- **الأنشطة**: شرائح حتى 8 ثم «عرض الكل»؛ النبذة مُطهّرة من الروابط/الأرقام مع «عرض المزيد»؛ قسم «بيانات النشاط العامة» مطوي ويُخفى إذا لم توجد بيانات.
- **الإعلانات**: المنشورة فقط، 12 في الدفعة عبر `useInfiniteQuery`، شرائح تصفية للتصنيفات ذات إعلانات فعلية، ولا استعلامات N+1 (التصنيفات والأنواع والوسائط تُحلّ دفعة واحدة).
- **الاختبارات**: `tsgo` نظيف؛ Playwright على 320/390/768/1366 لحالتي «غير متاح» والملف الحقيقي: `overflow = 0` وبلا أخطاء Console. اللقطات: `/tmp/browser/biz/biz_*.png`، `/tmp/browser/biz/missing_*.png`.
- **مؤجّل**: التحقق البصري لجلسة العضو المخوّل (بيئة الاختبار غير مسجّلة الدخول).

### فحص Responsive Regression النهائي (2026-08-03)

- **التغيير الوحيد**: `src/routes/businesses.$slug.tsx` — شبكة الإعلانات والهيكل الهيكلي (Skeleton) فقط. لا تغيير في قاعدة البيانات أو الدوال، ولا في أي مكوّن مشترك، ولا CSS عام.
- **Breakpoints المستخدمة**: الأساس للجوال (عمود واحد)، ثم `sm` (عمودان)، `md` (٣ أعمدة بعرض كامل بلا عمود جانبي)، `lg` (ظهور العمود الجانبي 19rem + عمودان في المحتوى)، `xl` (٣ أعمدة). لا JavaScript لقياس العرض في هذه الصفحة، ولا `scale`/`zoom`/`position: absolute` للتخطيط، ولا عرض أو ارتفاع ثابت بالبكسل، ولا `max-w` ضيق على الحاوية (`max-w-7xl` فقط).
- **الفروق المقصودة**: الجوال عمود واحد وأزرار `flex-1` بارتفاع لمس 44px؛ من `sm` الأزرار `flex-none` بعرضها الطبيعي؛ العمود الجانبي والالتصاق `lg:sticky lg:top-20` يظهران من `lg` فقط؛ الشريط السفلي `lg:hidden` والهوامش السفلية `lg:pb-0` في `MarketShell`.
- **المكونات المشتركة المفحوصة (بدون تعديل)**: `MarketShell` (Header/Footer/Bottom nav/Container)، `ListingCard`، `VerifiedBadge`، `Button`، الطباعة العامة في `styles.css`.
- **نتائج المقاسات السبعة** (`/businesses/qa-reports-test-82f84ac0`): 320×900، 390×852، 768×1024، 1024×768، 1280×800، 1366×768، 1440×900 — `scrollWidth = clientWidth` في كل مقاس (لا تمرير أفقي)، لا أخطاء Console، لا Layout Shift، الشريط السفلي `block` حتى 768 و`none` من 1024 وما فوق.
- **أعمدة الشبكة المقيسة**: 320→1، 390→1، 768→3، 1024→2، 1280→3، 1366→3، 1440→3.
- **تأكيد**: تعديل الجوال (عمود واحد) محصور في الأساس فقط، ولم يغيّر أي حجم على الكمبيوتر؛ `tsgo --noEmit` و`vite build` ناجحان.
- **اللقطات**: `/tmp/browser/bizresp/shots/{320,390,768,1024,1280,1366,1440}.png`.

## الوحدة العاشرة — اختيار الحساب النشط وتبديل الهوية (2026-08-03)

- **جرد المسارين**: `/select-account` = المسار الإداري القديم لنظام ERP (اختيار كيان `tenants` للوحة الإدارة). `/choose-account` = المسار المعتمد للسوق العام (الحساب الشخصي + المنشآت المخوّل بها). لا دمج بينهما، ولا روابط متقاطعة، وكل منهما يحدّث سياق جلسته فقط.
- **المصادر الخادمية**: `mkt_my_accounts` (القائمة) و`mkt_account_context` (تحقق عند الاختيار) بعد التحديث لإرجاع المدينة والنشاط المختصر فقط؛ الكيانات الموقوفة/المحذوفة والعضويات المنتهية تُستبعد داخل الدالة، والصلاحيات مسحوبة من `anon`.
- **العزل**: الاختيار لا يُعتمد من الواجهة: كل نقرة تستدعي `mkt_account_context` أولًا؛ إذا لم تُرجع صفًا (سحب عضوية أو إيقاف الكيان) يبقى المستخدم في الصفحة مع رسالة خطأ ولا يُخزَّن أي حساب نشط. تبديل الحساب يفرّغ الكاش المرتبط بالحساب السابق.
- **الترتيب والتصميم**: الحساب الشخصي أولًا في قسم مستقل، ثم «المنشآت المخوّل بها»؛ كل بطاقة: أيقونة/شعار، الاسم، التوثيق تحت الاسم، النوع · النشاط · المدينة · الصفة، وعلامة «الحساب الحالي». البطاقة كاملة هي هدف اللمس (77–102px مقيسة).
- **إنشاء منشأة**: إجراء ثانوي فقط (زر خفيف أسفل القائمة) يفتح صفحة مستقلة `/business/new` — لا Modal فوق الصفحة. الإنشاء لا يغيّر الحساب النشط، والعودة تكون لقائمة الحسابات مع الحفاظ على `next`.
- **منع Open Redirect**: `src/lib/safe-next.ts` (`safeInternalPath` / `isSafeInternalPath`) يُطبَّق مرتين: في `validateSearch` وعند التنقّل عبر `navigate({ href })`. يُرفض كل ما ليس مسارًا داخليًا بشرطة مائلة واحدة (`https://…`, `//host`, `/\host`, `javascript:`, مسافات/محارف تحكم) ويُستبدل بـ `/`. تم التحقق فعليًا: `?next=/search` يتبع المسار، و`?next=https://evil.example.com` ينتهي إلى الصفحة الرئيسية.
- **الاختبارات (Playwright بجلسة وRPC محاكاة)**: 320/390/768/1024/1280/1366/1440 → `scrollWidth = clientWidth` في كل مقاس، بلا أخطاء Console ولا أخطاء صفحة. أعمدة: 320/390 = 1، 768–1280 = 2، 1366/1440 = 3. حالة «لا منشآت» تعرض نصًا واحدًا واضحًا، وحالة سحب العضوية تبقى في الصفحة. لوحة المفاتيح: Tab + Enter يختار الحساب، وحلقة التركيز ظاهرة. العربية RTL والإنجليزية LTR مؤكدتان (`dir` يتغير فعليًا).
- **البناء**: `tsgo --noEmit` و`vite build` ناجحان. اللقطات: `/tmp/browser/acc/shots/`.

## إغلاق ملاحظتَي اختيار الحساب — 2026-08-03

نطاق العمل: إصلاح نقطتين فقط، دون إعادة تصميم الصفحة ودون نشر.

### 1) زر «إنشاء منشأة جديدة» → 404
- المسار `/business/new` مسجّل فعليًا في `routeTree.gen.ts` وفي `routes-map.ts`، والفحص الآلي أثبت عمله في الحالات الأربع: فتح مباشر، نقر من بطاقة الإجراء الثانوي، تحديث الصفحة (Refresh)، وزر الرجوع — بالعربية والإنجليزية (بلا 404).
- الاستنتاج: الـ404 الذي ظهر للمستخدم كان من شجرة مسارات قديمة في المعاينة (HMR/بناء سابق)، وليس رابطًا خاطئًا. بعد إعادة البناء زال تمامًا.
- تحسين مصاحب: عند إتمام إنشاء المنشأة يتم الآن تحديث كاش `["mkt","my-accounts"]`، فتظهر المنشأة في القائمة فورًا عند العودة، مع بقاء الحساب النشط دون تغيير.

### 2) رابط «تصفح السوق بدون حساب» = تجاوز الحساب النشط
- حُذف الرابط نهائيًا من صفحة اختيار الحساب: المستخدم المسجّل يعمل دائمًا تحت حساب واحد.
- الحالة (أ) لا يوجد حساب نشط: لا يظهر أي مخرج تجاوز، والاختيار إلزامي (الحساب الشخصي متاح دائمًا).
- الحالة (ب) يوجد حساب نشط صالح (فتح الصفحة من «تغيير الحساب»): يظهر زر «إلغاء والعودة إلى الحساب الحالي» فقط، ويعود إلى `next` بعد تطهيره.
- الحالة (ج) غير مسجّل: تحويل إلى `/auth?next=<المسار المطهَّر>` من `/choose-account` و`/business/new`، فيعود بعد الدخول إلى نفس الشاشة.
- الحالة (د) فقدان العضوية: يظل التحقق الخادمي (`mkt_account_context`) هو الحاكم مع رسالة انتهاء الوصول وإعادة الاختيار.
- Open Redirect: `next=https://evil.example` لا يُتبع؛ التنقل يبقى داخليًا (`safeInternalPath` مطبَّق في `validateSearch` وعند التنقل).

### 3) توحيد المسارات
- `/choose-account`: الحساب النشط للسوق العام (شخصي/منشأة).
- `/select-account`: اختيار مساحة العمل الإدارية (ERP) — شاشة مختلفة وظيفيًا، تعمل بشكل سليم ولم تُدمج.

### 4) نتائج الفحص
- 404: صفحة `/no-such-page-xyz` تعرض عنوانًا واضحًا مع `dir=rtl` عربيًا و`dir=ltr` إنجليزيًا.
- Responsive: 320 / 390 / 768 / 1024 / 1366 / 1440 على `/choose-account`, `/business/new`, `/u/$username`, `/businesses/$slug`, `/search` — لا تمرير أفقي ولا تداخل.
- أخطاء الـConsole: 0. ونجح `tsgo --noEmit` و`vite build`.

## الوحدة الحادية عشرة — إنشاء منشأة جديدة (2026-08-03)

**المسار الفعلي:** `/business/new` (SSR مُعطّل، `robots: noindex`)، يُفتح من زر ثانوي في `/choose-account` ويعود إليه فقط.

**مراحل النموذج (3 فقط، مؤشر «{n} من 3»):**
1. بيانات المنشأة: الاسم بالعربية، الاسم بالإنجليزية (اختياري)، نوع الكيان من قيم قاعدة البيانات فقط (`mkt_business_registry.entity_type`)، النشاط الرئيسي، أنشطة إضافية، المدينة، الموقع الإلكتروني، وصف مختصر، شعار مع معاينة وإزالة.
2. المسؤول والمستندات: الاسم النظامي، رقم السجل/الرقم الموحد، تاريخ الانتهاء، تواصل المنشأة، اسم المسؤول وصفته وهويته وجواله وبريده، تاريخ انتهاء التفويض للمفوّض، مستند السجل، ومستند التفويض عند عدم كون المسؤول مالكًا فقط.
3. المراجعة والإنشاء: ملخص فقط مع زر «تعديل» لكل مجموعة، إقرار إلزامي، ثم «إنشاء المنشأة».

لا يُسمح بالانتقال للمرحلة التالية مع خطأ إلزامي، والبيانات محفوظة في حالة الصفحة عند الرجوع بين المراحل.

**الدولة:** لا يوجد محدد دولة في الصفحة؛ تُؤخذ من دولة الحساب (`useAccountCountry`) وتُكتب في `country_id`، والمدن تُحمَّل لتلك الدولة فقط.

**الحقول العامة:** الاسم، نوع الكيان، النشاط، المدينة، الوصف، الموقع، الشعار.
**الحقول الخاصة:** رقم الهوية (`mkt_business_officers.id_number`، لا يُقرأ في الواجهة، ويُعرض بـ `maskLast4`)، أرقام السجل، جوال/بريد المسؤول والمنشأة، والمستندات.

**منع التكرار:** خادميًا عبر `mkt_business_number_taken` على أرقام مُطبَّعة (`normalizeOfficialNumber`: أرقام عربية/فارسية → ASCII، إزالة المسافات والشرطات، توحيد الأحرف)، مع قيد التفرد في `mkt_business_registry` كخط ثانٍ. الرسالة محايدة ولا تكشف المالك. الاسم ليس أساسًا للمنع.

**سياسة المستندات:** PDF/JPG/PNG/WebP فقط بفحص البصمة الحقيقية (`isRealDocument`)، حد 10MB، مسار عشوائي داخل Bucket خاص `MKT_BUCKET`، Signed URLs قصيرة فقط، ولا تخزين في LocalStorage.

**الإنشاء والعضوية:** `create_workspace` (SECURITY DEFINER) ينشئ الكيان وعضوية المالك الأولى؛ العميل لا يُرسل `tenant_id` ولا `owner_user_id` ولا دورًا، ولا يُضيف أعضاء آخرين. المستندات تُرفَع دائمًا مربوطة بطلب توثيق `mkt_verification_requests(status='pending')`.

**التوثيق:** لا تُرسَل `verified`/`verified_at`/`verified_by`/`verification_status`/ملاحظات الإدارة من العميل — القيم افتراضية خادميًا، والعلامة لا تظهر برفع المستندات.

**العودة:** بعد النجاح `/choose-account?created=<tenant_id>` مع `next` الآمن (`safe-next`)، وتُبرز المنشأة الجديدة مرة واحدة بإطار Primary + «تم إنشاء المنشأة…» دون تفعيلها تلقائيًا؛ الحساب النشط لا يتغيّر.

**الحفظ المؤقت:** لا Draft ولا LocalStorage — حالة الصفحة فقط، مع تنبيه ظاهر و`beforeunload` عند وجود تغييرات غير محفوظة (`unsaved-changes`).

**Breakpoints:** عمود واحد افتراضيًا، `md:grid-cols-2` للحقول المرتبطة، الحاوية `max-w-xl` ثم `lg:max-w-3xl` (~768px). ارتفاع الحقول `h-11` على الجوال و`sm:h-10` على الكمبيوتر، وأزرار بحد لمس 44px على الجوال. لا `h-screen` ولا `w-screen` ولا قياس بـ JavaScript.

**فحص التأثير المتقابل:** `BusinessQuickCreate` مستخدم في `/business/new` فقط (تحقق بالبحث)، فلا تغيير على نموذج الإعلان أو غيره. المكونات المشتركة المفحوصة: `Input`/`Label`/`Textarea`/`Button`/`Dialog` — لم تُعدَّل.

**الاختبارات المنفّذة:** `tsgo --noEmit` نجح، `vite build` نجح، وفحص Responsive على `/`، `/search`، `/business/new`، `/choose-account` عند 390 و1366: overflow = 0 ولا أخطاء Console.

**مؤجّل (سبب واضح):** التدفق الكامل داخل الصفحة (إنشاء مؤسسة/شركة، رفع شعار ومستندات، سجل مكرر، الضغط المتكرر، الظهور في قائمة الحسابات) لم يُختبر فعليًا لأن بيئة الاختبار حاليًا `signed_out`: كلا المساران يحوّلان إلى `/auth?next=…` كما هو مطلوب. تُستكمل هذه الحالات بعد توفّر جلسة اختبار.

**الملفات المتغيرة:** `src/components/marketplace/BusinessQuickCreate.tsx`, `src/routes/business.new.tsx`, `src/routes/choose-account.tsx`, `src/lib/mkt-business.ts`, `src/i18n/ar.json`, `src/i18n/en.json`. لا تغيير في قاعدة البيانات ولا في السياسات.
