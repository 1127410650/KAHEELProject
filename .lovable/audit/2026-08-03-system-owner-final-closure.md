# الإغلاق النهائي لنسخة مدير النظام — 03/08/2026

اختبارات فعلية فقط، بدون إعادة تصميم وبدون نشر.

## 1) حساب مدير النظام (o11339911@gmail.com)
- تسجيل الدخول → التوجيه المباشر إلى `/admin` (خادميًا عبر `mkt_my_platform_role`، لا شرط بريد في العميل).
- بقاء الوصول بعد التحديث وفي تبويب جديد (الجلسة المحلية + `guardSession`).
- «معاينة السوق» تفتح السوق العام مع بقاء جلسة الإدارة، وزر العودة للإدارة يعمل.

## 2) العزل والصلاحيات
- المستخدم العادي والمشرف المحدود: منع `/admin` ومنع تنفيذ RPC الإدارية (400/403).
- إصلاح تسريب البريد بين الكيانات في `profiles` بسياسة تعتمد `shares_active_tenant`
  (النتيجة: صف واحد بدل 92 لحساب QA).
- سحب `EXECUTE` من `PUBLIC`/`anon` لـ 22 دالة إضافية + تأمين `mkt_admin_listing_reports`.

## 3) الإجراءات الحساسة وسجل التدقيق
- تقييد منشأة ثم رفع التقييد: نجحا (204) وسُجّلا في `mkt_restriction` بسبب إلزامي.
- `audit_log` غير قابل للتعديل أو الحذف (`audit_log_no_update` / `audit_log_no_delete`).

## 4) المستندات والملفات
- سلة `attachments` خاصة: لا قراءة عامة (400) ولا توقيع لغير المصرح (404 لكل من anon
  والمستخدم غير المخوّل) — لا كشف مستندات حساسة.

## 5) المقاسات 320 → 1440
- 7 مقاسات × 8 صفحات إدارة: **0 overflow**.
- الجوال: الجداول مخفية بالكامل (tablesVisible = 0) والعرض بطاقات، القائمة الجانبية Drawer تفتح فعليًا.
- لا يوجد شريط سفلي للسوق داخل الإدارة (0)، وهو موجود في السوق العام كما يجب.
- إصلاحات لمس هذه الجلسة: شعار الهيدر وزر «معاينة السوق» ومفاتيح الإعدادات أصبحت ≥ 44px.
  إعادة الفحص بعد الإصلاح: لا عنصر تفاعلي أقل من 40px في `/admin` و`/admin/settings`.
- السوق العام (390 و1366): الرئيسية والبحث تعملان بدون overflow، ومسارات `/dashboard/*`
  تحوّل حساب الإدارة إلى `/choose-account` كما هو مصمم.

## الخلاصة
نسخة مدير النظام مغلقة ومستقرة. الخطوة التالية: إصلاح إرسال الإعلان للمراجعة وتنظيف الملفات اليتيمة.
