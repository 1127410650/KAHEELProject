{
  "name": "kaheel-appointments-standalone",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prepare:source": "node scripts/bootstrap.mjs prepare",
    "dev": "npm run prepare:source && python3 -m http.server 4173",
    "test": "node scripts/bootstrap.mjs test",
    "build": "node scripts/bootstrap.mjs build"
  }
}
