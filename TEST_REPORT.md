# تقرير اختبار كَحيل مواعيد المستقل

تاريخ التنفيذ: 8 أغسطس 2026

- 24/24 اختبارًا ناجحًا.
- تغطية 12/12 تصنيفًا، بحسابين صالحين لكل تصنيف.
- اختبار إنشاء موعد أو دخول طابور لكل واحد من حسابات مقدمي الخدمة الـ24.
- اختبار تفرد معرفات مقدمي الخدمة والخدمات وأرقام الجوال الخيالية.
- اختبار OTP دون كلمة مرور برمز العرض `123456`.
- اختبار البحث القريب والترتيب بالمسافة ونطاق البحث.
- اختبار حجز الوقت ومنع الموعد المشغول.
- اختبار عدم تكرار الدور النشط وتسلسل أرقام الطابور.
- اختبار انتقالات حالات الموعد والطابور.
- اختبار عدم وجود أي اعتماد على `public.mkt_*` أو `public.profiles`.
- اختبار تفعيل RLS لكل جداول الأعمال المكشوفة.
- نجاح `npm run build` وفحص بناء JavaScript.

لم يُطبّق أي تغيير على قاعدة بيانات سوق كَحيل، ولم يُدمج الفرع في `main`.
